Resume the Perkins v2 video-archival platform after /clear. FIRST read CONTINUATION-2026-07-06.md in
full — it has the complete state (huge build+harden session; branch feat/platform-v2; live API+SPA on
commit 2ff19cb) and the resume quick-start. Follow docs/ENGINEERING_RULES.md (R1 core coverage ≥97% +
behavioral tests, R2 architect+critic review per wave, R3 100% IaC git→apply — Terraform for GCP,
scripts/deploy.sh for the image, scripts/apply_migrations.sh for schema, R4 no drift).

AUTH GOTCHAS (critical): the `.env` sets GOOGLE_APPLICATION_CREDENTIALS=infra/vertex-dev-sa.json (a
READ-ONLY SA). For ANY gcloud / Secret Manager / Cloud Scheduler / deploy command you MUST
`unset GOOGLE_APPLICATION_CREDENTIALS` first (else 403), then
`export CLOUDSDK_AUTH_ACCESS_TOKEN=$(gcloud auth application-default print-access-token)`. Keep that env
var SET (to vertex-dev-sa.json) only for Vertex/DB jobs. ADC expires mid-session — if gcloud says
"Reauthentication failed", ask Jon to run `!gcloud auth application-default login` (that also drops the
cloud-sql-proxy → restart it: `env -u GOOGLE_APPLICATION_CREDENTIALS /tmp/cloud-sql-proxy
video-archival-and-content-gen:us-central1:video-archival-and-content-gen-pg --port 5432` in background).
DB_URL is in /tmp/perkins_dburl.txt. DEPLOY GATE: verify the SPA with `npm run build` (exit 0), NOT just
`tsc --noEmit` — a failing build silently keeps a stale bundle live.

TOP TASK — fix the ingest QUEUE trigger. Re-queuing a video (Dashboard Retry → ingestion_runs
status='pending') never processes because NOTHING auto-executes the `ingest` Cloud Run Job
(jobs.ingest_worker) — there's no scheduler for it (empty `gcloud run jobs executions list --job=ingest`).
Stuck: WLX7kUWleto (stage transcript, pending since 03:13). Add an automatic trigger: simplest is an
`/internal/ingest` endpoint (INTERNAL_SECRET-gated, bounded batch — mirror `/internal/crawl-comments` in
api/app.py) + a Cloud Scheduler job (mirror the crawl-comments block in infra/main.tf; every 15–30 min);
consider render/article queues too. CAVEAT: stage `transcript` needs Whisper on cerberus — verify it's up
(`ansible-playbook local_llm.yml -e reclaim_gpu_from_whisper=false && ansible-playbook whisper.yml`;
WHISPER_URL=http://cerberus-ai:9000/asr) or ingest will trigger but fail transcription.

THEN, as capacity allows (details + status in the continuation):
- FAQ answer backlog is grinding on Vertex (~3,930/4,541 answered, resumable: LLM_BACKEND=vertex
  `python -m jobs.prime_backlog --answers 6000`). When done, re-run POST /faq/publish-wordpress (page 7895)
  to refresh the site FAQ with the consolidated set.
- Reconcile R4 drift: secret versions, reels-bucket IAM, and the 3 Cloud Scheduler jobs were applied via
  gcloud AND written to infra/main.tf — a terraform plan may need a one-time import of the crawl-comments
  scheduler. Run scripts/drift_check.sh.
- Jon action (external): API-restrict the Firebase web key to Identity Toolkit in the GCP console.

VERIFIED WORKING (don't redo): comment-crawl cron (every 2h, HTTP 200 confirmed, rotates all 841 videos);
vault-backed secrets + all 8 Config health checks green; publishing (WP creds live); all 34 articles at
SEO/AIO 100; FAQ semantic consolidation (4,541); the whole console UI is finally deployed (hard-refresh).
Commit/deploy as you go; don't push unless asked.

Resume the Perkins platform + Ez-Bids build after a Claude RESTART (done to load the
Knowify MCP). Read IN ORDER: CONTINUATION-2026-07-11.md, the memory index (auto-loaded,
esp. knowify-integration, perkins-pricing-verified, deploy-not-concurrency-safe),
docs/ENGINEERING_RULES.md (R1–R5 binding), then `mcp__jarvis-memory__sync` + my_tasks.

DO THESE FIRST, IN ORDER:

1. KNOWIFY MCP (reason for the restart). It's registered (`claude mcp list` → knowify,
   "Needs authentication"). Jon authenticates via `/mcp` → knowify → Authenticate →
   browser login with Knowify admin creds (OAuth; no passwords). Then use the read-only
   tools (Platform Knowledge, Query Knowify). FOR BULK DATA (task #15 migration): run
   `python scripts/knowify/knowify_oauth.py` (browser login once) then
   `python scripts/knowify/knowify_pull.py` → /tmp/knowify_data. Analyze the pulled
   invoices/milestones/payments to validate + finalize the invoicing/numbering/QuickBooks
   model. First pull reveals pagination shape — tweak knowify_pull.py if _summary.json shows gaps.

2. T&C AI-FAQ (task #17) is DONE + COMMITTED (db01b57), gated (core 100%), migration 0029
   applied, backend deploy kicked off pre-restart. VERIFY the deploy landed: `gcloud run
   services describe api --region=us-central1 --format='value(spec.template.spec.containers[0].image)'`
   should show :db01b57 (or newer). If older (restart interrupted it), re-run bash
   scripts/deploy.sh (run_in_background). Deferred to JB3: formal R2, Tim's real-T&C sign-off
   (core/tc_seed.py is DRAFT v0.1), per-tenant T&C from DB, SPA surfacing.

3. THEN AUTOPILOT the approved plan (.omc/plans/ralplan-perkins-jobdocs-billing-DRAFT.md):
   JB1 material price book (task #12) → JB2 measurements/details → JB3 proposal engine
   (task #14; T&C-FAQ is a component) → JB4/JB5 invoicing+e-sign+QuickBooks (task #15;
   use the Knowify import). Also Ez-Bids W1 (tenant-1 binding + Perkins-smoke gate) → W2.

NON-NEGOTIABLE DISCIPLINE:
- Gate = the CI-equivalent gate above, NOT a Docker-having local gate. Subagents = FILE
  EDITS ONLY, never git; non-overlapping boundaries; main lane commits + gates on a clean tree.
  Implementation subagents = sonnet; opus for plan/review/security.
- Deploy is NOT concurrency-safe — one deploy at a time, via run_in_background (not `&`);
  verify image sha vs HEAD after. strict=True LIVE (stamp sessions). Migrations additive .sql.
  R3 infra via terraform only. Output-compressor garbles piped JSON → write to file, parse separately.
- R2 (architect+critic) on every wave; fix HIGH/critical before marking done.
- Do NOT relitigate locked/approved decisions (JB plan APPROVED; Ez-Bids plan APPROVED).
  Surface hard-to-reverse prod actions.

STATE: everything from the 2026-07-10/11 sessions is LIVE (Estimator v2, Ez-Bids W0 CORS,
invite email+logo, video-unavailable, estimator admin cleanup, Tim link fix, Knowify tooling).
T&C AI-FAQ is UNCOMMITTED in the tree (step 2). Waiting on Jon (Knowify /mcp auth; Meta/FB
access for social) + Tim (golden files, Jupiter/Naples pricing, pm_incentive, T&C sign-off,
audio walkthrough — follow-up logged for 7/13).

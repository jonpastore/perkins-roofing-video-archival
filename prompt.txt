Resume the Perkins FULL-FUNNEL platform build (plan APPROVED 2026-07-08). Read IN ORDER first:

1. CONTINUATION-2026-07-08.md            — full handoff (state, locked decisions, gotchas)
2. docs/superpowers/plans/2026-07-08-full-funnel-plan.md — the approved plan (waves F0–F6)
3. Memory index (auto-loaded) — esp. perkins-full-funnel-reorg, perkins-ezbids-proposal,
   perkins-knowify-teardown, perkins-squarequote-review
4. docs/ENGINEERING_RULES.md             — R1–R5 binding
5. mcp__jarvis-memory__my_tasks          — live human blockers (#315–331)

MISSION, in this order:

PHASE 0 — Documentation set (BEFORE any code), under docs/superpowers/specs/full-funnel/:
  - PRD-<section>.md per sidebar section (knowledge-base, marketing, estimating, quoting, admin):
    user stories, acceptance criteria, non-goals, differentiators (Quoting must beat Knowify on
    self-serve multi-templates, revisions, good-better-best tiers).
  - TRD-F0..F6.md per wave: data models, APIs, migrations, RLS/GCIP mechanics, TEST PLAN FIRST,
    rollout/rollback. F2 embeds Exhibit-B pricing rules + the 5 golden-file ±$0.01 acceptance
    (incl. Dade+Broward=HVHZ, per-county overrides). F2b = Google Solar API measurement service
    (+ manual fallback). F3 = proposal builder + e-sign-lite + Gotenberg + Knowify migration.
  - DDD.md: bounded contexts (Tenancy/Identity, Corpus/KB, Content/Marketing, Pricing/Estimating,
    Quoting/Proposal, Measurement), aggregates, entities/value objects, ubiquitous-language
    glossary, context map with integration seams.
  Draft with sonnet subagents (clear file-ownership boundaries), R2 review (architect + critic),
  fix HIGH/critical, commit + push the set.

PHASE 1 — Execute F0 (thin tenancy: tenants table + tenant_id default 1 everywhere, ORM
discipline, NO RLS/GCIP yet) then F1 (sidebar IA: KB · Marketing · Estimating · Quoting + Admin
config-tab shell; keep existing route keys) — each per its TRD.

NON-NEGOTIABLE DISCIPLINE:
- TDD, FAIL-FIRST, ALWAYS: write the test → run it → confirm it fails FOR THE RIGHT REASON →
  minimal implementation → green → refactor. No implementation before a red test. Every wave,
  every feature. R1's 100% core gate is the floor, not the method.
- Full gate before any "done": ruff clean + pytest 100% core coverage (run backgrounded to a
  /tmp log with EXIT marker) + `cd web && npm run build` for SPA changes.
- Implementation subagents = sonnet; opus only for plan/architecture/security/final verification.
- Commit + push at each green milestone. scripts/deploy.sh requires a CLEAN tree (run via bash).
- NO prod DB changes without my explicit permission (then scripts/apply_migrations_connector.py;
  needs fresh ADC which only I can mint).
- Do NOT relitigate locked decisions (continuation §3): GCP · Postgres RLS+tenant_id · GCIP
  (Perkins stays project-pool) · Cloudflare ingress + app.perkinsroofing.net · Solar API
  measurements (no LiDAR/drones, no Google-Earth scraping) · e-sign-lite · Gotenberg ·
  royalty-FREE music only · no payments/accounting/CRM/native-iOS in v1 · eaglepoint source is
  reference-only, never merged into this repo.

COMMERCIAL CLOCK: F3 (Quoting/Proposals) is the ~3-week deliverable to Tim; critical path
F0 → (F1 ∥ F2) → F3. Bias prioritization toward it; if squeezed, cut reminders/leads — never
templates/revisions/tiers/e-sign. Tim's email (docs/2026-07-08-tim-email.md) is drafted — golden
files + pricing confirmations arrive via him and gate F2's contract-grade signoff only.

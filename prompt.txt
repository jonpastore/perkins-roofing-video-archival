Resume the Perkins full-funnel platform. F0–F6 are ALL BUILT + DEPLOYED to prod +
deepsec-hardened + infra-reconciled. Read IN ORDER first:

1. CONTINUATION-2026-07-09.md            — full handoff (current state, the active task, gotchas)
2. Memory index (auto-loaded)            — esp. perkins-full-funnel-reorg
3. docs/ENGINEERING_RULES.md             — R1–R5 binding
4. mcp__jarvis-memory__sync then my_tasks — live human blockers (Perkins v2 project)

PRIMARY TASK — C1 Part 2 (the tenant-2 gate; local task #38, full checklist in the continuation):
Make the last request-path sessions tenant-aware, then flip strict=True (GLOBAL flag on the
SessionLocal factory). In order:
  1. Thread a stamped `db` through the retrieval chain (app/store.vector_search,
     app/retrieval.hybrid_search+search, app/answer.ask+answer_faq) + update callers
     (api/app.py /search + /ask, api/routes/faq.py answer_faq call). They query RLS-forced
     chunks/content_graph UNSTAMPED today.
  2. core/tenant_loop.for_each_tenant: switch the tenant-ENUMERATION session from raw
     SessionLocal → PlatformSessionLocal (per-tenant fn sessions are already stamped).
  3. Audit job top-level sessions (jobs/ingest_worker.py:36, jobs/article_job.py:649/692).
  4. Flip strict=True in app/models.py (~line 638).
  5. Full gate + tenancy PG suite + DEPLOY + PROD SMOKE (/search /ask /status + a CRUD route +
     one job) — SQLite can't exercise strict, so prod smoke is the real test.
THE PROVEN PATTERN (Part 1 done, commit d9e2e5b): tenant route → add
`db: Session = Depends(get_db_session)` after require_role, drop `with SessionLocal()`, dedent,
commit→flush, insert tenant_id=db.info["tenant_id"]. Platform-table route → PlatformSessionLocal.
Reference: api/routes/customers.py, api/routes/measurements.py.

ALSO BUILDABLE (no external dep): #321 Contract-FAQ engine (web/src/pages/ContractFaq.tsx is a
stub — build the T&C→LLM-FAQ backend, reuse the FAQ/JSON-LD/safety-gate machinery). #40 deepsec
M/L: L2 template-render sandbox review + pip-audit/npm-audit in CI.
GATED ON JON (don't attempt): H1 edge (needs Cloudflare token + LB / jarvis #330 DNS), Tim's
estimator pricing + clips + ToS, YouTube OAuth mint, deposit/ownership reconcile.

NON-NEGOTIABLE DISCIPLINE:
- TDD fail-first ALWAYS. Full gate before "done": ruff clean (`ruff check core adapters api jobs`)
  + `pytest tests/ --cov=core --cov-fail-under=100` (background to /tmp log + EXIT marker —
  headroom garbles inline pytest output) + `cd web && npm run build` for SPA changes.
- Implementation subagents = sonnet; opus for plan/architecture/security/final verification only.
- Commit + push at each green milestone. scripts/deploy.sh needs a CLEAN tree + `gcloud auth login`
  (CLI token, separate from ADC); frontend needs `firebase login --reauth`. Migrations via
  scripts/apply_migrations_adc.py (ADC-only). R3: infra ONLY via terraform, never gcloud-by-hand.
- Do NOT relitigate locked decisions (continuation §"Locked decisions"). Jon has been authorizing
  applies/deploys ("do it / greenfield") — but still surface hard-to-reverse prod actions.

CRITICAL GOTCHAS (see continuation for detail): RLS GUC needs NULLIF(current_setting(n,true),'')
on pooled connections; migrations can't DML RLS-forced tables under the app role (use DDL DEFAULT);
get_db_session 403s without tenant context; PlatformSessionLocal never raises (platform tables);
strict=True is global + SQLite no-ops the event so only prod smoke validates it.

Resume perkins-roofing/video-archival after a /clear.

READ FIRST: CONTINUATION-2026-07-17.md and docs/KNOWN_GAPS.md — full state.
docs/ENGINEERING_RULES.md (R1-R5) is binding. HEAD 16a662b (or later); branch main, clean.

CREDENTIALS — THIS IS DIFFERENT NOW (2026-07-17):
- All Google automation runs under a NON-EXPIRING service account. Before any gcloud/terraform/
  firebase: `export GOOGLE_APPLICATION_CREDENTIALS=/home/jon/.config/gcloud/perkins-deploy-sa.json`
  (also in ~/.bashrc). NO interactive login is EVER needed — do NOT ask Jon to reauth; if a
  Google call 401s, re-export the SA key, don't start an OAuth flow. Key backup: Secret Manager
  `perkins-deploy-sa-key`. (Note: sourcing .env resets GOOGLE_APPLICATION_CREDENTIALS to a
  nonexistent infra/vertex-dev-sa.json — re-export the SA key after any `source .env`.)

PRIMARY GOAL (Jon's ask): endpoint coverage + negative tests, driven by a live 500.
A prod 500 on POST/PUT /quoting/customers/{id}/properties ("value too long for type character
varying(2)", state="Florida") was fixed in 16a662b by adding Pydantic max_length. The LESSON:
the suite runs on SQLite, which IGNORES VARCHAR(n) limits, so oversized-string input passes in
tests but 500s on prod Postgres — a whole class of bugs the tests can't see. This is SYSTEMIC:
~90 length-bounded Column(String(N)) columns + 21 route files with create/update/request Pydantic
schemas, most likely missing max_length. Do:
  A. max_length AUDIT across all 21 route schemas — every str field mapping to a bounded DB
     column gets max_length matching app/models.py. Parallelize across route-file clusters (agents).
  B. NEGATIVE TESTS for every endpoint: oversized (422), missing-required (422), wrong-type,
     not-found (404), cross-tenant/authz (403/401). Run the constraint-sensitive ones under the
     Postgres harness (scripts/test_pg.sh) — SQLite hides VARCHAR + some constraint violations.
  C. Push endpoint coverage toward 100% (R1 behavioral-validation via these negative tests).
  Grep `Column(String(` in app/models.py, map table→schema, fix + test. Deploy per cluster.

THEN the remaining gaps in docs/KNOWN_GAPS.md (confirm drift scope with Jon first):
1. Least-privilege the deploy SA — replace roles/owner with scoped roles (secretmanager.admin,
   run.admin, cloudscheduler.admin, monitoring.editor, cloudsql.client, artifactregistry.writer,
   cloudbuild.builds.editor, iam.serviceAccountUser), verify a deploy still works, drop owner,
   codify in infra/main.tf.
2. Migration-replay robustness — swap scripts/apply_migrations.sh's naive ;-split for sqlparse
   (or psql -f); verify a full replay against prod runs clean (idempotent).
3. Terraform drift (7 resources: cloudflare WAF/rate-limit/routing, zone settings, api domain
   mapping, gotenberg) — review intent WITH JON, then apply deliberately or codify. Do NOT
   blind-apply (changes prod networking/WAF).
4. Real Pexels key (Jon provides): `printf '<key>' | gcloud secrets versions add pexels-api-key --data-file=-`.
5. Only if #319 progressed: OAuth capture UI — build web/src/pages/Connections.tsx, wire
   OAUTH_STATE_HMAC_KEY value + OAUTH_REDIRECT_BASE into deploy.sh, register /oauth/{platform}/callback
   redirect URIs per provider (see docs/plans/2026-07-17-social-app-registrations.md).

WHAT'S ALREADY LIVE (don't rebuild): the OAuth health ALARM (integration_status + 30-min scheduler +
Cloud Monitoring, emails admins on integration-broken); production-readiness panel (dashboard top +
Admin Config; prod eval = READY, 0 blockers, 4 expected warnings); Phase 2 comment platform column
(migration 0040); A2 speaker tracking; prompt-to-clip; clip-studio parity; comments E1 gate.

OPERATE:
- Prod DB: `/tmp/cloud-sql-proxy --port 5432 video-archival-and-content-gen:us-central1:video-archival-and-content-gen-pg &`
  then DB_URL=postgresql+psycopg://app:$(gcloud secrets versions access latest --secret=db-password)@127.0.0.1:5432/perkins.
  cloud-sql-proxy v2 binary is at /tmp/cloud-sql-proxy (re-download v2.14.1 if gone).
- Deploy (non-interactive now): `bash scripts/deploy.sh` (API+jobs; needs CLEAN git tree); SPA:
  `cd web && npm run build && npx --no-install firebase deploy --only hosting --project video-archival-and-content-gen`.
- Migrations 0039+0040 applied to prod. Apply future migrations DIRECTLY (full replay has a
  documented ;-splitter gap). RLS: platform-level tables (integration_status, oauth_state_nonces,
  tenant_offboard_log) are exempt via _RLS_EXEMPT_PLATFORM_TABLES in app/models.py — tenant tables
  get auto-RLS from the _rls_on_create hook.
- Offload bulk/non-security work to the FREE fleet: `llm -m gpt-oss-120b "..."` (sync, ~0.3s) and
  `mcp__hermes__submit_task` (async, free CF tier). Security-critical code + review stay on Claude.

ENVIRONMENT GOTCHAS (each cost real time — still true):
- articles/videos are RLS-protected: session.info["tenant_id"]=1 or queries error with
  'unrecognized configuration parameter "app.tenant_id"'. Only tenant 1 exists.
- `pgrep -f <pat>` / `pkill -f <pat>` MATCH THIS SHELL'S OWN command line — use ps + a bracketed
  grep (`ps -eo pid,cmd | grep "[c]loud-sql-proxy"`), never pkill -f a pattern in your own command.
- No foreground `sleep` (blocked); for "wait until X" use Bash run_in_background + an until-loop.
- WP REST needs WP_APP_PWD from Secret Manager; .env WP_PWD is the WEB login and 401s on REST.
  Prod WP is a GoDaddy STAGING site (myftpupload host) — the readiness panel flags this (#317 cutover
  clears noindex). Don't touch the 123 pre-existing published posts.
- PERKINS_ENV=prod requires EMBED_BACKEND=LLM_BACKEND=vertex + GOOGLE_CLOUD_PROJECT + a non-sqlite DB_URL.

Delegate parallelizable build work to worktree agents (model=sonnet). WATCH FOR: agents this session
repeatedly went idle WITHOUT committing and forked from stale bases — always verify the worktree is
committed + rebased, run the tests YOURSELF, then merge. Verify in prod (proxy + real facts), don't assert.

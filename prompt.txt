Resume perkins-roofing/video-archival after a /clear.

READ FIRST: CONTINUATION-2026-07-22.md (full state). docs/PRODUCTION_CUTOVER_PLAN.md (cutover
checklist + model routing + the CF prod flip). HEAD == origin/main, clean. Deployed prod image
== HEAD (d1e25b5 or newer — check `git rev-parse --short HEAD` vs the api image).

STANDING RULE — BINDING model routing (Jon 2026-07-22):
- All creative/content (articles, llms.txt, portfolio write-ups, scope-of-work AI rewrite) →
  CLOUDFLARE free-tier llama (LLM_BACKEND=cloudflare, @cf/meta/llama-3.3-70b-instruct-fp8-fast),
  VALIDATED by Vertex/Gemini. Local gpt-oss was 1-time priming only — prod does NOT depend on it.
- App code (toggles, estimator, connectors) → build with SONNET or qwen-coder 3.6, review with OPUS 4.8.
- .env is ONLY a secure key transport into the vault — NEVER a runtime config source. Site URL etc.
  come from the admin config (PlatformConfig), not env.

FIRST STEPS on resume:
- Start the Cloud SQL proxy: `/tmp/cloud-sql-proxy video-archival-and-content-gen:us-central1:video-archival-and-content-gen-pg --port 5432 &`
  (needs GOOGLE_APPLICATION_CREDENTIALS=/home/jon/.config/gcloud/perkins-deploy-sa.json). DB reads:
  raw psycopg + `SET app.tenant_id='1'`, or jobs.article_job._stamped_session(1).

STATE: 61 articles LIVE + validated on new staging https://1228404.us6.myftpupload.com (30 new +
31 refreshed; FAQ+Video schema, no /blog/, footer, 200 service links). Ready for Wendy's review.
Prod on d1e25b5, LLM_BACKEND=vertex (CF adapter dormant), SEO submission OFF, email test-mode.
CF Workers-AI verified: token=Secret Manager cloudflare-api-token (has Workers AI), account
3303058f686721d6877d6d1e8b8a448c. WP staging app pw vaulted (wordpress-app-password v3), WP_USER=jon,
admin PlatformConfig WP_URL=1228404. o365 mail MCP wired (drafts to DeGenito Outlook).

PICK UP (build to the routing policy above):
1. llms.txt generator (AIO: robots allowlist + llms.txt manifest; CF-gen; do NOT touch Rank Math
   sitemap/robots). Plan: scratchpad/aio-and-cf-plan.md.
2. Repair/re-roof toggle + scope-of-work field (app code sonnet/qwen; the template+AI-rewrite runs
   on CF llama). Design brief: scratchpad/repair-sow-design.md. Gaps: repair card can't make a
   proposal; no toggle in Quoting.tsx yet.
3. Portfolio automation (Wendy): 30-field Google Sheet -> Avada Portfolio (cats Commercial/
   Residential/Construction, tags=location, skills=roof-type). CF write-ups from CompanyCam+proposal+
   YouTube; permissions human-gated. Doc/Sheet IDs in the O365 "Meeting notes and info" thread.
4. CF PROD FLIP (terraform: inject CLOUDFLARE_API_TOKEN into Cloud Run api+jobs + set
   LLM_BACKEND=cloudflare; apply + deploy). See docs/PRODUCTION_CUTOVER_PLAN.md §2.
5. Deploy SPA if any web/ changes shipped: cd web && npm run build && firebase deploy --only hosting:app.

WAITING ON WENDY (wendy@webpowermarketing.com): staging<->prod parity (breadcrumbs on prod 7/16 not
staging); prod Rank Math config (no dup FAQ schema); review the 61; project data. WAITING ON TIM:
6 pricing items ($1185/$1435 labor CONFIRMED). WAITING ON JON: CF prod-flip go; prod WP app password
at cutover; SEO submission creds (IndexNow key + Google Indexing SA).

OPERATE: deploy = `set -a; source .env; set +a; export GOOGLE_APPLICATION_CREDENTIALS=/home/jon/
.config/gcloud/perkins-deploy-sa.json; bash scripts/deploy.sh` (CLEAN tree; deploy.sh hardcodes
vertex+prod, bakes WP_URL from .env but runtime uses admin-config WP_URL). Prod smoke =
.venv/bin/python scripts/prod_smoke.py. R6: every commit updates docs + Jarvis + memory.

GOTCHAS: prod LLM_BACKEND flip to cloudflare is DELIBERATE (terraform, not automatic). Never run a
writer executor + deploy.sh concurrently (deploy needs clean tree). Agent-tool worktrees can spawn
STALE (pre-merge) — use `git worktree add <path> main` for a current base. Cloud SQL proxy must be
running for DB work.

Resume perkins-roofing/video-archival.

READ FIRST: CONTINUATION-2026-07-29.md, then core/wireproxy.py and
docs/2026-07-28-companycam-credentials.md.

HEAD 3665020, pushed, CI + deploy green. DEPLOYED platform:3665020.
Prod configs jupiter v27 / miami v29 / naples v27. Terraform applied, drift gate exit 0.

Confirm:
  git log --oneline origin/main..HEAD          # expect empty
  gcloud run services describe api --region us-central1 \
    --format='value(spec.template.spec.containers[0].image)'
  bash scripts/plan_as_ci.sh                   # expect "plan exit code: 0"
  gcloud run jobs executions list --job=archive --region=us-central1 --limit=2

WHAT IS SETTLED — do not re-derive
  - The video pipeline runs END TO END from Cloud Run: enumerate (07:00 ET, newest 60/tab) ->
    backfill (only rows missing dates) -> archive (07:30 ET, shortest-first, batch 5) -> ingest.
    Catalog 841 -> 856. archive_uri is filling with real gs:// objects.
  - Archive downloads MUST go through the userspace WireGuard tunnel. YouTube bot-blocks
    datacenter egress 15/15; cookies do NOT help (measured with the jar verified loaded).
    A blocked exit is STICKY per config, so rotation across configs is the strategy, not retry.
  - CompanyCam is LIVE — an APPLICATION KEY (app 16351, Read & Write, no expiry), not a PAT.
  - Articles: 474/474 compliant. The 4 pillar_link clusters are fixed.
  - Miami overhead $4,250/day is Jon's call, live and verified. Closed.
  - No VM is needed. compute.googleapis.com is still disabled; the stack stays serverless.

THE WORK, ranked

1. ⚠️ CUTOVER TO PRODUCTION WORDPRESS. Everything the platform publishes goes to STAGING
   (PlatformConfig.WP_URL = https://1228404.us6.myftpupload.com, read from the prod DB — it is
   the single source of truth, there is no .env fallback). Prod perkinsroofing.net has had no
   platform content since 2026-07-02; staging's newest is 7/24. The vaulted app password is a
   STAGING administrator and 401s on prod. Needs: a prod WP application password vaulted, then
   one PlatformConfig.WP_URL change, then verify ONE article round-trips (context=edit, product
   UA — GoDaddy's WAF strips Authorization from default library UAs).
   Until this happens every content improvement is invisible to Perkins' customers.

2. WATCH THE ARCHIVE BACKLOG DRAIN. 13 of 856 still have archive_uri NULL. At ARCHIVE_BATCH=5
   nightly it clears in ~3 days, then the channel's 1-2 uploads/day keep pace easily.
   If it fails with "all N egress config(s) exhausted", Proton ranges have been blocked —
   refresh the wireguard-configs secret with new server configs. That is maintenance.
   Then DELETE the youtube-cookies secret: it is retained but unused, and it is a full Google
   session (SID/SAPISID are .google.com-scoped, so Gmail/Drive/Cloud Console too).

3. WENDY. A draft is sitting UNSENT in Jon's DeGenito Outlook (wendy@webpowermarketing.com, cc
   tim@perkinsroofing.net) asking where project pages should live and whether she has a schema.
   The portfolio work is blocked on her answer: 9 public projects are PAGES under /portfolio/
   (parent 2358) while our 13 generated drafts are avada_portfolio CPT — different URL spaces.
   Audit of the 9: 0/9 JSON-LD, 0/9 video, 0/9 service/location links, 4 images sharing one alt.

4. Smaller: accent items priced but NOT selectable in the SPA (extra_line_items is API-only) ·
   o365 MCP refresh token EXPIRED (use gmail-enhanced for that same mailbox) ·
   proposal-reminders-daily scheduler PAUSED with no recorded reason ·
   4 test files still drop_all at teardown — see GOTCHAS, converting them is NOT mechanical.

GOTCHAS
- "WORKS ON MY LAPTOP" FAILED FOUR TIMES TODAY. Docker on the workstation uses the HOST network,
  so a container test proves NOTHING about Cloud Run egress. yt-dlp and deno were both missing
  from the image while working locally.
- A job with per-item try/except MUST NOT exit 0 when every item failed. archive_job reported
  {'archived': 0, 'errored': 15} and exit(0) — Cloud Run showed green and would have nightly.
- CalledProcessError.__str__ prints ONLY the command. Carry stderr in the message, and prefer
  the ERROR lines — the tail alone surfaces a trailing WARNING instead of the actual cause.
- pytest imports EVERY test module before running any test, so a drop_all teardown tears tables
  out from under modules that init_db() at import and only DELETE rows. Create-but-never-drop.
  Converting the remaining 4 caused 50 failures (for_each_tenant reads the tenants table;
  drop-vs-truncate differ) and was REVERTED. Isolation bugs only exist in combination — run the
  FULL suite, and capture pytest's own exit code (piping to `tail` masks it).
- yt-dlp --cookies exports the ENTIRE browser jar: 1803 cookies / 439 domains incl. 1password.com.
- Never leave a secret in scratch. Write to Secret Manager, then shred -u.
- terraform apply BEFORE a deploy that adds a job — deploy.sh runs `gcloud run jobs update`,
  which fails if the job does not exist yet.
- gcloud --impersonate-service-account prints a WARNING to stderr; use 2>/dev/null.
- `gh run list --limit 1` may return a PREVIOUS run. Match the SHA before believing it.
- Cloud SQL proxy 127.0.0.1:5432 (NOT a local container — there is no local postgres), db
  perkins, user app; every query needs set app.tenant_id='1'.
  PW=$(gcloud secrets versions access latest --secret=db-password).
- CI gates on `ruff check core adapters api jobs` FIRST, then pytest --cov-fail-under=97.
  scripts/ and tests/ are NOT ruff-gated. Full suite ~6 min local. SPA: cd web && npx tsc -b.
- bash scripts/deploy.sh refuses a dirty tree (R3-ENFORCE) — that is deliberate.

Resume perkins-roofing/video-archival after a /clear.

READ FIRST: CONTINUATION-2026-07-20-pm.md (full state + the 3 tasks in order).
docs/ENGINEERING_RULES.md (R1-R6) is binding. HEAD == origin/main, clean.

DO THESE, IN ORDER:
1. Build the Quoting config panel — AdminConfig → Quoting is an empty PlaceholderCard
   (web/src/pages/AdminConfig.tsx:108): proposal templates, T&C library, deposit policy,
   reminder cadence. Back it with tenant settings (see core/tenant_settings.py + the
   /admin/tenant/settings/* pattern other sub-tabs use).
2. Wire the T&C/FAQ/AI-prompt pages + the fairness review into proposal generation.
   - core/proposal_review.py already exists (review_proposal -> {pass, issues[]}: flags
     contradictions/unfair/predatory/prompt-injection/FL-legal; fail-safe). Wire it into
     core/proposal_gen.py / api/routes/proposal_gen.py after doc assembly; block/surface
     high-severity issues.
   - core/proposal_render.py already has a tc_faq_items slot; proposal_doc_render.py has a
     marketing_appendix append hook. Render T&C page(s) + FAQ page (core/contract_faq.py
     turns tc_text into FAQ) + an AI-prompts page.
   - BLOCKER: real T&C wording is pending Tim (not in repo). Build the plumbing with a
     clearly-marked placeholder so it flows through when his text lands.
3. AV end-to-end render test + tuning — render ONE real approved clip through
   jobs/render_job.py _apply_track_a_engines (reframe->captions->censor->transcode) on a
   real source video; verify auto-censor mutes audio + masks captions; tune censor tail_pad,
   scene-gap threshold, caption styles, reframe focus. These engines were unit-tested but
   NEVER driven end-to-end on a real MP4.

CRITICAL — OFFLOAD MORE THAN LAST TURN: use `llm -m qwen3.6-coder "..."` (sync) and
mcp__hermes__submit_task(model_tier="cloudflare") (async) for EVERY self-contained draft —
prompts, prose, pure modules, form/component scaffolds. Keep on Claude only security wiring,
repo integration, and verification. ALWAYS review a CF/qwen draft before applying (they
re-invent conflicting code — the real files never match their diffs; write clean yourself
using their content).

PROD STATE: HEAD 749a38d; deployed API image ddc6dab (suggest-500 fix, TikTok refresh persist,
article no-op, non-root Docker all live); SPA current (UI fixes + Clip Studio help modal live);
core/proposal_review.py committed but UNWIRED + undeployed. app 200 · api/health 200. terraform
+ ansible clean. Alerting active (alert_email=dmarc@perkinsroofing.net) — Jon must click the
GCP channel-verification email.

CREDS: export GOOGLE_APPLICATION_CREDENTIALS=/home/jon/.config/gcloud/perkins-deploy-sa.json
(also in ~/.bashrc). Cloudflare token: TF_VAR_cloudflare_api_token="$(gcloud secrets versions
access latest --secret=cloudflare-api-token)".

OPERATE: deploy API+jobs = bash scripts/deploy.sh (CLEAN tree, SHA-tagged). SPA = cd web &&
npm run build && npx --no-install firebase deploy --only hosting:app --project
video-archival-and-content-gen. Drift = bash scripts/drift_check.sh. Prod smoke =
.venv/bin/python scripts/prod_smoke.py. R6: every commit updates docs + drift_check + Jarvis +
memory.

OPEN (unchanged): B9 QB account_id collision (#358, HIGH before QB live); tenant-2 hardening
(#359); CompanyCam photos reader (#360); FB/LinkedIn/X/YT real publishers (only IG/TikTok
publish today); infra B6 decisions (#363); live posting blocked on Meta/TikTok app review (#319).

GOTCHAS: relaxing a Pydantic Literal at a trust boundary removes validation (500); f"{None:.1f}"
raises (the suggest-500 root cause — guard null timestamps); terraform needs the CF token
injected or the provider masks drift as a creds error; never pkill -f a pattern that matches
your own shell; qwen/CF drafts don't match the real repo files — extract their content, write clean.

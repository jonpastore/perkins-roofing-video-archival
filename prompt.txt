Resume perkins-roofing/video-archival.

READ FIRST:
    CONTINUATION-2026-08-02-night.md   ← START HERE. The backlog audit + what it found underneath
    CONTINUATION-2026-08-02-eve.md     Tim's four answers, shipped
    docs/2026-08-02-perkins-execution-plan.md   the ralplan-reviewed plan (v2)

main pushed, tree clean, CI green, ZERO open PRs.
DEPLOYED AND VERIFIED: API platform:4cb73a5 (sha matches main), SPA /assets/index-C3NGvMvU.js.
Migrations through 0054. Live configs: jupiter v29, miami v30, naples v29.

Confirm before trusting any of this:
  git log --oneline -1 && git status --short
  gh run list --limit 4                        # MATCH THE SHA
  gh pr list                                   # expect none open
  .venv/bin/ruff check core adapters api jobs
  .venv/bin/python -m pytest tests/ --cov=core --cov-config=.coveragerc --cov-fail-under=97
    # ~10m. CI runs THIS (the whole tree).
  bash scripts/plan_as_ci.sh                   # expect "plan exit code: 0"

⛔ STAY ON STAGING for WordPress (prod's newest post is 2026-07-02 — see #456)
⛔ DO NOT send client email (EMAIL_SEND_MODE=test; Jon sends it)

════════════════════════════════════════════════════════════════════════════════
▶ THE ONE THING TO KNOW
════════════════════════════════════════════════════════════════════════════════
The backlog was lying in one direction. A sweep of all 64 open tasks found SIX describing work
already shipped — #430 #449 #418 #385/#386 #409/#410, the last two fixed by ONE commit (4fd78f7)
that closed neither. VERIFY EVERY TASK AGAINST THE CODE BEFORE WORKING IT. Two greps lied this
session and were caught by reading the match: a keyword hit is not a done task.

R6.3 is now a HOOK (.githooks/, core.hooksPath). Every commit needs one of:
    Closes #453          finished it — REQUIRES a `Verified:` line
    Refs #429 60%        progress
    No-Task: <reason>    deliberately not task work
post-commit syncs Jarvis in real time; a red ruff gate holds the task at 90% instead of closing.
It CANNOT evaluate acceptance criteria (prose) and CANNOT run the coverage gate (R7). CI is the
verdict.

════════════════════════════════════════════════════════════════════════════════
▶ NEXT UP — verified not-done at the end of the last session
════════════════════════════════════════════════════════════════════════════════

1. #359 tenant-2 hardening. strict=True present, require_role_db in 2 routes, BRANCH FK ABSENT
   (grep -c 'ForeignKey("branches' app/models.py = 0). The branches table is at app/models.py:686.
   The FK + create_config validation is what remains.

2. #360 CompanyCam READER. api/routes/companycam.py has ONLY @router.post("/webhook") — the mirror
   is write-only. CompanyCamPhoto exists. Build the reader; the PAT half stays blocked on Tim.

3. #417 thirteen low-slope gaps (docs/low-slope-comment-audit-2026-07-25.md). SORT THEM FIRST into
   implementable-from-config vs needs-Tim, or it stalls halfway.

4. #342 eval harness (evals/ absent) — a project, not a task. #402 aluminum link (5 min).

5. #452 project-bid commission — price_project computes NONE. Plumbing buildable; the BASE waits
   on #451 (is General Conditions commissionable?).

ALSO OPEN
  - #456 THE WORDPRESS CUTOVER GATES ALL CONTENT WORK. 11 unchecked items in
    docs/PRODUCTION_CUTOVER_PLAN.md, in no task until now. Jon owns the prod WP app password + the
    mu-plugin re-install; Wendy owns 3. Prod's newest post is 2026-07-02 and everything publishes
    to staging, so #378 #379 #382 #384 #407 are shelfware until this clears.
  - #319 social dev-app registrations: 2-4 weeks of EXTERNAL calendar, 0% since filed, and blocked
    on JON'S DECISION — under whose identity do the apps register, Tim's or DeGenito's? An
    afternoon of forms creates that fact permanently.
  - THE LETTER TO TIM. 13 questions queued for one man. Rebuilt rule: corpus-exhaustion for
    INCLUSION (zero hits in HIS material), his own artefact for PHRASING — send `D19 =
    SUM(B19:B20)×1.15`, never the words "General Conditions", which already failed once. ≤6
    questions, ≤400 words, each with a stated default so silence resolves to a decision.
    Separately, as a TELLING with no question mark: the permit divergence we owe him (#457,
    +2.3% → +4.2%, further from his own bid) and #431's margin-squeeze findings.
  - #436 is DONE but its finding is for Tim: 95% is NOT reachable from 29 homes (one home = 3.4
    points). He offered 20 more on the 7/27 call — cheapest next move.

GOTCHAS
- ⚠️ A keyword hit is not a done task. #331's "solar" = solar panels as a roof FEATURE, not the
  Google Solar API. #410's "placeholder" = prompt text, not the guard.
- ⚠️ _build_low_slope NEVER reads flat_squares — the mixed-roof block is gated on
  slope_type=="sloped". Moving area into flat_squares on a low-slope quote DELETES it from the
  price (45 sq tpo_adhered: $35,050 -> $8,250, 76% under). This shipped and R2 caught it.
- ⚠️ measurements.total_sq is AMBIGUOUS by provenance: Tim's sheet = sloped-only, RoofR =
  pitched+flat. The split now resolves SERVER-side in _quote_input_from_request (shared by /quote
  AND /project-quote); 6 measurements still carry no split and get a `split_unknown` stamp.
- ⚠️ A default is only a default where nothing supplies a value (the SPA sends commission_rate on
  every quote, so config.commission_rate() has no caller from that screen).
- ⚠️ Bump _PDF_TEMPLATE_VERSION whenever DEFAULT_TEMPLATE_HTML changes or cached PDFs keep the old
  bytes.
- ⚠️ A reproduction path needs a different default from a creation path (permit_count).
- ⚠️ KNOWIFY: deliverables carry ContractId, NOT ProjectId. Money is in CENTS.
- ⚠️ CI runs `pytest tests/` (whole tree); CI ruff scope is ONLY `core adapters api jobs`.
- ⚠️ CI DEPLOYS THE SPA NOW (deploy.yml, since 2026-08-02). Still verify the SERVED bundle.
- ⚠️ MIGRATIONS ARE APPLIED BY HAND. apply_migrations_adc.py IGNORES DB_URL (always prod) and has
  NO ledger — it replays from 0013. Confirm it reaches DONE.
- ⚠️ REVIEWER AGENTS MUST RUN WITH run_in_background: false. R2 needs BOTH — the critic found the
  architect's fix was aimed at the wrong LAYER this session.
- ⚠️ LOCAL MODELS ARE NET-NEGATIVE ON REVIEW. docs/2026-08-01-local-model-review-postmortem.md.
- ⚠️ Do NOT `source .env` before GCS work. DB_URL in .env is sqlite; prod is
  postgresql+psycopg://app:$(gcloud secrets versions access latest --secret=db-password)@127.0.0.1:5432/perkins
  over the proxy, and scripts need PYTHONPATH=.
- ⚠️ SEARCH THE MAILBOX, not just transcripts.
- Cloud SQL proxy: setsid nohup ~/bin/cloud-sql-proxy \
    video-archival-and-content-gen:us-central1:video-archival-and-content-gen-pg --port 5432 &
- Terraform without the invalid_rapt reauth:
    cd infra && unset GOOGLE_IMPERSONATE_SERVICE_ACCOUNT
    export GOOGLE_APPLICATION_CREDENTIALS=$HOME/.config/gcloud/perkins-deploy-sa.json
    export USER_PROJECT_OVERRIDE=true GOOGLE_CLOUD_QUOTA_PROJECT=video-archival-and-content-gen
    export TF_VAR_cloudflare_api_token="$(gcloud secrets versions access latest --secret=cloudflare-api-token)"
- Jarvis: ONE project, `perkins-roofing-video-archival` (id 2013), named for the repo. The naming
  rule is enforced in jarvis@b1add89 so it cannot re-fragment.
- R7-R10 in docs/ENGINEERING_RULES.md are binding. R6.3 is now hook-enforced.

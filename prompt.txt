Resume perkins-roofing/video-archival after a /clear.

READ FIRST: CONTINUATION-2026-07-21.md (full state). docs/perkins-buildout-plan-2026-07-21.md
(the plan). HEAD == origin/main, clean, HEAD 4227442. Deployed API image = 41472dc.

STANDING RULE (every task): USE LOCAL/FREE MODELS FIRST to save Claude tokens.
- Code drafts: `llm -m qwen3.6-coder "..."` (sync) or mcp__hermes__submit_task(model_tier="cloudflare").
- Article generation: LOCAL `gpt-oss-120b` (non-think) via litellm, VERTEX validates. Never a cloud generator.
- Whisper: LOCAL faster-whisper large-v3 on the RTX 2060 (set LD_LIBRARY_PATH to the venv nvidia cublas/cudnn dirs).
- Claude (opus) = orchestrate + verify only. Execution → SONNET subagents (not opus).
- Article data from transcripts/KB ONLY. Invent nothing. Check everything.

STATE: Estimator + Quoting done and deployed. Clips fixed. Article pipeline rebuilt for
local grounded generation: FAQ+Video-only schema (Rank Math owns the rest), internal links
(no /blog/), YouTube footer, dense answer-first (cluster 1000/pillar 1500), numeric-grounding
gate that BLOCKS invented figures. Article-gen commits after 41472dc are OPT-IN (prod stays
LLM_BACKEND=vertex) so they are NOT deployed and are behavior-neutral until you flip to litellm.

PICK UP (no fixed order — ask Jon or pick by priority):
1. LAUNCH THE FULL ARTICLE RUN (drafts only, publishes nothing): batch-generate all pillars +
   2-3 clusters each in metal-first priority, local gpt-oss-120b + Vertex validate, grounded,
   stored as drafts. Multi-hour local-GPU job. Needs Jon's go (it produces content for review).
   Prereq changes are DONE (schema scope, internal links, numeric gate, length).
2. SEND THE WENDY REPLY (drafted in the last chat, ready). Then build to her import spec.
3. ESTIMATOR 07-20 items (Jarvis #385-386): repair options (shingle/tile/metal/flat) with
   time-based pricing + tear-off/re-install DAYS inputs + daily labor rates; fix missing
   roof-type-selection bug.
4. o365 MAIL MCP (all local): add create_draft/sendMail to jarvis/o365-connector/graph_client.py,
   write jarvis/o365-mcp/server.py (mirror memory-mcp), register in ~/.claude.json, restart — so
   Tim/Wendy drafts land in the degenito mailbox instead of jpastore79 gmail.
5. SEO/AIO submission layer: IndexNow + Google-Indexing-API ping on publish (Rank Math already
   owns sitemap/robots) + dashboard on/off toggle. Model on ~/projects/degenito/seo-aio.
6. NEW PAGES: metal-warranty-checker (setback tool https://perkins-setback.web.app/ → WP plugin,
   salinity cross-ref USGS + oceansciences, brackish canals count); thumbnail gallery (AI picks a
   good frame, article image = chosen thumbnail); project-posting UI (proposal + CompanyCam →
   Avada Portfolio).

WAITING ON TIM (draft email in jpastore79 gmail — move to degenito to send): per-branch overhead;
gutter hangers; downspout separate+rate; Verea "S"/Caribbean field-tile costs; other FBC low-slope
deltas; confirm the josh 49-clause T&C is current.

WAITING ON YOU (Jon): OMC plugin reinstall to 4.15.6 via /plugin, then /oh-my-claudecode:setup
--global (preserve). Bring the STAGING WordPress into parity with prod before publish testing.

OPERATE: deploy API+jobs = bash scripts/deploy.sh (CLEAN tree). SPA = cd web && npm run build &&
firebase deploy --only hosting:app. export GOOGLE_APPLICATION_CREDENTIALS=
/home/jon/.config/gcloud/perkins-deploy-sa.json. Prod smoke = .venv/bin/python scripts/prod_smoke.py.
Prod pricing config: NON-DESTRUCTIVE version bumps only (see reconcile_*_pricing.py); low-slope v5,
tile-brands v6. R6: every commit updates docs + Jarvis + memory.

GOTCHAS: prod LLM_BACKEND MUST stay vertex until you deliberately flip to local; never run a writer
sonnet executor and deploy.sh concurrently (deploy needs clean tree); Tim's LIVE sheet outranks
Exhibit B for pricing (wood_deck OH 50, FBC polyglass 450); numeric gate deletes the sentence of any
figure it can't ground — good for liability, watch for over-trim on spelled-out numbers.

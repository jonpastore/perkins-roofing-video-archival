Read CONTINUATION-2026-08-12-pm.md first. It has the full state. Its §1 has the A/B numbers,
§4 is your task list, §5 is the unresolved question that decides whether Phase 2 ships.

CONTEXT IN ONE LINE: two features are BUILT AND WIRED but UNCOMMITTED and UNVERIFIED — the full
test suite and the R2 review were still running when the last session ended.

  Phase 1  core/audio_enhance.py    new `wind` option (highpass=f=90 + softer afftdn)
  Phase 2  core/video_redact.py     NEW — pixelates operator-marked PII regions
           plus wiring in core/render_spec.py, jobs/render_job.py, api/routes/clips.py

═══════════════════════════════════════════════════════════════════════════════
DO THESE IN ORDER
═══════════════════════════════════════════════════════════════════════════════

1. VERIFY THE SUITE. Capture the exit code to a file BEFORE any pipe and read it back — a
   task-notification saying "exit code 0" reports the WRAPPER's exit, not pytest's, and that
   mistake cost a full cycle earlier today:

     .venv/bin/python -m pytest tests -p no:warnings > /tmp/s.log 2>&1; echo "EXIT=$?" > /tmp/s.exit

   pyproject sets addopts="-q". Do NOT pass -q again or the summary line disappears and a green
   run looks truncated.

2. RUN R2 (architect + critic) on the five files above. The previous review was launched and never
   read. Fix every HIGH/CRITICAL before committing. R2 caught a CRITICAL in the CompanyCam work
   earlier today that would have emptied every gallery, so do not skip it.

3. ANSWER §5 — THE QUESTION THAT MATTERS MOST.

   render_job applies redaction EARLY, then speech_cleanup / reframe / captions / fuse run after.
   `reframe` crops and repositions the frame. The mosaic is burned in at fixed SOURCE-frame pixel
   coordinates.

   Prove, by rendering end-to-end with reframe=True AND a redact region and looking at the output:
     - does the mosaic still cover the PII after reframing?
     - can reframe pan/scale such that the PII re-enters frame from outside the redacted box?

   A redaction a later stage undoes is WORSE than no redaction, because the operator believes it
   worked. If it is broken, the fix is to apply redaction LAST (after reframe) or to transform the
   region coordinates through the same crop.

   Also unresolved: redact_regions are source-frame pixels, and nothing records what resolution the
   operator marked against. build_redact_cmd already accepts frame_w/frame_h — consider storing
   them with the regions so a mismatched preview size is caught rather than silently misplacing
   every box. (That exact mistake happened by hand last session; validation caught it only because
   the true dimensions were passed.)

4. COMMIT — two separate commits, Phase 1 then Phase 2. Trailer: `No-Task: <reason>` alone on its
   line, plus:
     Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

═══════════════════════════════════════════════════════════════════════════════
RULES
═══════════════════════════════════════════════════════════════════════════════

- DO NOT deploy.
- DO NOT touch the metal warranty page — awaiting Tim/Marco/Josh's reply.
- DO NOT release the 7 `held` scheduled_content rows — Jon's call.
- ./ab-review/ is gitignored demo media. Do not commit it. Do not delete it — Jon may not have
  reviewed it yet.
- Label every finding VERIFIED FROM DOCUMENT or INFERENCE. Do not merge the two. Five corrections
  in one session yesterday all came from stating an inference with the confidence of a fact.
- If Phase 2 turns out to be unsafe because of §5, SAY SO and stop rather than shipping it. A
  reasoned "this does not work yet" is worth more than a merged privacy feature that leaks.

IF JON HAS ALREADY GIVEN A VERDICT ON THE A/B: apply it. If he said the wind difference is too
small to matter, the next lever is DeepFilterNet3 as an adapter behind the same `wind` flag — but
do not add that dependency without his go-ahead.

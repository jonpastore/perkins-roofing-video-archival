Resume perkins-roofing/video-archival.

READ FIRST: CONTINUATION-2026-07-29-pm.md, then CONTINUATION-2026-07-29.md (the morning
session), then core/pii.py and core/portfolio_criteria.py.

HEAD 023d59d, pushed, CI green, DEPLOYED platform:023d59d (api service verified).
Prod migrations applied through 0050. Cloud SQL now enforces
idle_in_transaction_session_timeout=5min.

Confirm before trusting anything below:
  git log --oneline -1 && git status --short
  git log --oneline origin/main..HEAD          # expect empty
  gcloud run services describe api --region us-central1 \
    --format='value(spec.template.spec.containers[0].image)'
  bash scripts/plan_as_ci.sh                   # expect "plan exit code: 0"
  gh run list --limit 4                        # MATCH THE SHA; "deploy skipped" == nothing shipped

WHAT IS SETTLED — do not re-derive
  - CompanyCam is fully mirrored: 3,684 projects / 155,975 photos / 11,635 videos in
    companycam_projects|_photos|_videos (prod Cloud SQL, metadata only — media stays on their
    CDN). Sync is INCREMENTAL: a run with nothing new skips ~3,679 projects in ~2 min.
  - PII is a HARD GATE. core/pii.py + core/portfolio_criteria.py refuse a publish on any
    address / unit / ZIP / phone / email / GPS in ANY surface (body, meta, title, image ALT,
    JSON-LD captions, scope lines) and on a title that reads as a person. Calibrated over all
    26,063 real scope lines: 42 flags, 0 false positives. Do not "simplify" these regexes
    without re-running that corpus check — the first version had 499 false positives from
    "ste" matching inside "STEEL".
  - Policy: city and neighbourhood ARE allowed. Anything narrowing to a property or person is
    not. "Jim Malooly Delray Beach Roof" blocks even with all permissions granted.
  - Write-ups are DETERMINISTIC and grounded in Knowify contract deliverables. No LLM writes
    project prose: the record holds ~6 facts. No quantities are ever published (units are
    provably wrong: "7550 Squares" = 755,000 sq ft) and no prices.
  - Portfolio UI is finished: 3 tabs (Project / Media / SEO+AIO) + full CRUD. Projects are rows
    (portfolio_projects, migration 0050), seeded once from the old hardcoded CANDIDATES.
  - The coverage gate is honest now (.coveragerc precision = 2). CI reports 97.43%.
  - single_flight commits on acquire; Cloud SQL enforces
    idle_in_transaction_session_timeout=5min. Do not revert either — together they fix the
    lock pile-up that blocked an ALTER TABLE.

THE WORK, ranked

0. ⛔ STAY ON STAGING. Jon, 2026-07-29: *"stay on staging till we clear client approval."*
   Do NOT change PlatformConfig.WP_URL, do NOT vault a prod WP app password, do NOT publish
   anything to perkinsroofing.net. STAGING (https://1228404.us6.myftpupload.com) is the correct
   and intended target until Perkins approves. This is a business gate, not a missing task —
   the cutover mechanics are understood and parked:
     - one PlatformConfig.WP_URL change + a prod WP application password, then verify ONE
       article round-trips (context=edit, product UA — GoDaddy's WAF strips Authorization from
       default library UAs). docs/PRODUCTION_CUTOVER_PLAN.md has the ordered checklist.
   Keep reading every "verified live" claim in the docs as STAGING, because it is.

2. ⚠️ RE-UPLOAD wp-mu-plugin/perkins-jsonld.php to STAGING (Jon — a file upload to WP hosting,
   not something deploy.sh does). Staging is the only one that matters while we stay on staging;
   prod gets it at cutover, and it is already on the cutover checklist. The live version registers _perkins_jsonld for
   post type `post` ONLY, so project pages store and render NO schema: measured, publish
   returned jsonld_stored:false and the meta was absent on read-back. git now covers post +
   avada_portfolio + page. An inline <script> is NOT a workaround — WordPress strips it from
   post content on an application-password write, silently, 200 OK.

3. BOTH EMAILS ARE SENT — Jon sent them himself 2026-07-29 21:21/21:23 UTC, with his own
   edits. Do not re-send or re-draft. WATCH FOR THE REPLIES; each unblocks something:
   - Tim/Marco/Josh, "One question about the $1,400/day before I set it". The question: is
     $1,400/day charged per job-day, or split across the jobs running that day? ($1,400 x 250
     working days = $350k vs the $336k annual burn — right for ONE job at a time; measured
     concurrency ~1-1.7 jobs/working day in 2025, 1.5-4.3 in 2024.) His answer gates flipping
     overhead_basis. Evidence: scripts/margin_check.py — the rebuild says +4.2% median margin,
     NOT the -0.4% the superseded 7/28 draft claimed.
     ⚠️ Jon ADDED an ask: "Can you try some quotes and tell me if it's working like you expect
     and if the numbers make sense?" — so expect feedback on the live Quoting UI, not just a
     yes/no on the overhead question.
   - Wendy (cc Tim), "Project pages — where should they live". URL space (/portfolio/ pages vs
     the avada_portfolio CPT our publisher targets) + whether she has a schema.
     ⚠️ Jon DECIDED the interim in the email: "I'm going to use the article standards for now
     until I hear from you." That is exactly what core/portfolio_criteria.py implements
     (FAQPage + ImageObject + VideoObject, nothing Rank Math owns, unique alts, in-content TOC),
     so no change is needed unless she answers differently.

4. FINISH THE PROJECT PAGES once #2 lands: verify JSON-LD renders in <head> on a published
   project, then set featured_media (needs the image uploaded to WP as an attachment — ours are
   CompanyCam-CDN URLs today), and get CompanyCam URLs for the 2 candidates that have none
   (now editable in the UI).

5. Smaller: accent items priced but NOT selectable in the SPA (extra_line_items is API-only) ·
   o365 MCP refresh token EXPIRED (use gmail-enhanced for that mailbox) ·
   proposal-reminders-daily scheduler PAUSED with no recorded reason · 4 test files still
   drop_all at teardown (converting them caused 50 failures once and was reverted).

GOTCHAS
- AFTER ANY WRITE, READ IT BACK AND COUNT. Four things reported success today while doing
  nothing: the sync mirrored 50 of 3,684 projects (pagination stopped on a short page and
  /v2/projects caps per_page at 50); publish returned 200 as "skipped-exists" so curation
  changed nothing; an inline JSON-LD <script> was stripped entirely; and the coverage gate
  passed on rounding. None of them raised.
- CORPUS-VALIDATE EVERY HEURISTIC. Unit tests were green while the PII detector had a 98%
  false-positive rate on real data.
- Terraform: APPLY BEFORE PUSH. The deploy workflow has a drift gate and will refuse if git
  carries an unapplied change. (I committed the Cloud SQL flag first and it correctly refused.)
- `pkill -f "<pattern>"` matches its own shell — a waiter killed itself twice. Use a PID.
- A short page from a paginated API does NOT mean the last page.
- app/models.py is NOT ruff-gated (CI lints core adapters api jobs); its E702s are pre-existing.
- tests/core/test_avatar_script.py fails if tests/core runs ALONE (needs another module to
  create `tenants`). Pre-existing, passes in the full suite.
- Cloud SQL proxy 127.0.0.1:5432 (no local postgres), db perkins, user app; every query needs
  set app.tenant_id='1'. PW=$(gcloud secrets versions access latest --secret=db-password).
- CI gates `ruff check core adapters api jobs` FIRST, then pytest --cov-fail-under=97. Full
  suite ~6 min on CI, much slower on this box. SPA: cd web && npx tsc -b.
- bash scripts/deploy.sh refuses a dirty tree (R3-ENFORCE) — deliberate.
- Never leave a secret in scratch: write to Secret Manager, then shred -u.

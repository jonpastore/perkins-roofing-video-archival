Resume the Perkins v2 platform after a /clear. Read IN ORDER:
CONTINUATION-2026-07-11-eve.md (canonical — the Knowify data-mirror was BUILT, DEPLOYED to
prod, and SEEDED with the full real dataset this session), then the auto-loaded memory index
(esp. knowify-mirror-build, knowify-oauth-outage, billing-schema-numbering, perkins-v2-platform),
then docs/ENGINEERING_RULES.md (R1–R5 binding). Then `mcp__jarvis-memory__sync` + my_tasks.

STATE (all committed + PUSHED on main, tree clean): The full Knowify data-mirror is LIVE IN PROD.
- Migrations 0030+0031+0032 applied to Cloud SQL (invoice counter untouched at 18732).
- Terraform (targeted) created knowify-sync/keepwarm Cloud Run Jobs + `0 8-18 * * *` ET scheduler
  + knowify-tokens secret + IaC log-metric alert. API+jobs deployed. Web redeployed → the admin
  console "Knowify" tab is LIVE at https://video-archival-and-content-gen.web.app.
- PROD SEEDED with the full real Perkins data: 7,404 customers (18 inactive via customers.is_active),
  4,484 invoices (4,037 paid; #18732=$6,678 paid), 4,629 payments, 8,747 synthesized ledger events.
  Money-verified (counter never advanced). Seeded via scripts/knowify/mcp_pull.py (Claude MCP OAuth
  token → Knowify MCP endpoint, headless) + an in-region Cloud Run job (WAN was too slow).

DO THESE (priority order — from CONTINUATION-2026-07-11-eve.md "Outstanding"):
1. HOURLY AUTO-SYNC can't pull yet: Knowify's REST OAuth 500s on the RFC 8707 `resource` binding
   (their server bug — knowify-oauth-outage memory; support email DRAFTED not sent in
   docs/perkins-analysis/knowify-support-oauth500.md). Decide: wait for Knowify fix, email support,
   OR wire the sync job to the MCP-token stopgap (token must move to Secret Manager + refresh). The
   mirror is a point-in-time snapshot until this is resolved.
2. promote_items reads wrong Knowify field names (rec["Name"] etc.) — same bug class as the client
   fix; audit vs the real item schema (items are secondary; Tim's sheet is authoritative for costs).
3. Pre-existing TF drift (cloudflare placeholder token, gotenberg, domain mapping) — NOT from this
   work; a full scripts/drift_check.sh is not clean because of it. Reconcile separately (Jon-gated).
4. Confirm R1 core coverage ≥97 in CI (an ad-hoc stripped-env run showed ~94% because pre-existing
   billing/proposal PG tests didn't run together; the new core/knowify/* modules are 100%).

NON-NEGOTIABLE DISCIPLINE:
- Gate = pytest --cov=core (≥97) + scripts/validate_*.py + ruff on a clean tree. Subagents = FILE
  EDITS ONLY, never git; sonnet for implementation, opus for money/migration/OAuth/security/review.
- R2 (architect+critic) every wave; fix HIGH/CRITICAL before done. Money/legal code stays on Claude.
- Deploy: git→apply (R3). Migrations via scripts/apply_migrations_connector.py (needs `gcloud auth
  application-default login`; sets app.tenant_id=1 for RLS seeds). Terraform: TARGET the knowify
  resources to avoid the pre-existing cloudflare/gotenberg drift. deploy.sh needs a clean tree.
- Knowify money units: REST=DOLLARS (no ÷100); MCP/raw layer payments=CENTS (÷100). AIA skipped.
- Analysis artifacts (client PII) in docs/perkins-analysis/ (gitignored). Do NOT commit PII.

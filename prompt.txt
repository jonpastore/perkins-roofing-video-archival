Resume the Perkins v2 platform build. FIRST read CONTINUATION-2026-07-06-pm.md in full — it has the
complete state, deployed status, outstanding items, commands, and gotchas. Everything is committed on
branch feat/platform-v2 (HEAD 41c13e5) and DEPLOYED (Cloud Run API image 41c13e5, SPA on Firebase);
terraform is drift-clean. Prod migrations (articles.focus_keyword, user_settings) already applied.

Follow docs/ENGINEERING_RULES.md — especially R3-ENFORCE: NO direct deploy. Commit first (scripts/deploy.sh
refuses a dirty tree), deploy via scripts/deploy.sh (API+jobs) + `firebase deploy` (SPA) + `terraform apply`
(infra). Run scripts/drift_check.sh and reconcile any drift via `terraform import` + apply, never by hand.

AUTH: `.env` sets GOOGLE_APPLICATION_CREDENTIALS=infra/vertex-dev-sa.json (read-only SA, for Vertex/DB jobs).
For gcloud / Secret Manager / Scheduler / storage commands, run in a shell WITHOUT that var (unset it) so
ADC uses the jon@perkinsroofing.net account. Start the Cloud SQL proxy before local DB/job work:
  /tmp/cloud-sql-proxy video-archival-and-content-gen:us-central1:video-archival-and-content-gen-pg --port 5432 &
  export DB_URL="postgresql+psycopg://app:$(gcloud secrets versions access latest --secret=db-password)@127.0.0.1:5432/perkins"
Verify the SPA with `cd web && npm run build` (exit 0), not just tsc.

PRIORITIES (full detail in the continuation §Outstanding):
1. Task 27 — batch-regenerate all 34 articles to pass Rank Math + republish to WordPress. Job
   jobs/regen_articles_seo.py works (validated on wall-flashings → 100/100) but the generator only
   guarantees 14/15 checks — keyword DENSITY isn't hit. Either (a) run the batch now for the 14/15 win
   (LLM_BACKEND=vertex .venv/bin/python -m jobs.regen_articles_seo), or (b) first add a density-targeting
   refine step in jobs/article_job.generate_scored_article so it passes all 15, then run it. The Articles
   SEO panel's new click-to-fix already fixes density per-article on demand. ASK before batch-modifying
   live WordPress content.
2. YouTube Post-to-YouTube: if Tim owns the channel, mint the youtube.force-ssl refresh token
   (scripts/youtube_oauth_setup.py), store as secret youtube-oauth-refresh-token, wire into deploy.sh/infra
   (docs/YOUTUBE_REPLY_OAUTH.md).
3. GSuite invite dropdown: if the Workspace super-admin grant landed, finish domain-wide delegation
   (docs/GSUITE_DIRECTORY_SETUP.md).
4. Retire the cerberus Whisper node in ansible/whisper.yml (STT is fully cloud) so drift_check is fully green.

VERIFIED THIS SESSION (don't redo): cloud STT via GCS-output batch (97-min podcast = 362 segs); auto-drain
ingest cron; comment-crawl cron (populates comment_count); email suite (WYSIWYG/templates/signatures/header/
AI-draft loop); ClipStudio intro/outro videos; Articles 15 Rank Math checks + click-to-fix; WP author policy
(Tim Kanak, 6 posts reassigned); scheduling WP links; search-topics toggle fix; logs IAM fix; IaC drift
reconciled. Commit/deploy as you go; don't push unless asked.

Resume perkins-roofing/video-archival after a restart.

READ FIRST: CONTINUATION-2026-07-24-pm.md (full state). HEAD == 738b638 (clean, pushed).
SCRATCH: /tmp/claude-1000/-home-jon-projects-perkins-roofing-video-archival/fc25f81c-006d-444d-a7cf-26ac94fa2737/scratchpad
  (per-session; if gone, durable state = DB + git + ~/perkins-corpus/)

WHERE THINGS STAND (2026-07-24 pm):
- ARTICLES DONE: 375 compliant on STAGING; all 62 metal articles now LIVE. QC pass applied
  (relativized prod-absolute links, repointed 169 dead cluster→pillar links, added down-links —
  commit 12966ff / scripts/relink_hub.py). Gate fully deterministic (critic + video auto-heal).
  Re-run after any new batch: scripts/backfill_article_videos.py + scripts/relink_hub.py --apply.
- GMAIL-ENHANCED MCP fixed to run locally (cwd bug patched in ~/.claude.json — restart applies it;
  copied cerberus's live DeGenito token). Read Tim's mail from ~/projects/gmail-enhanced-mcp.
- TIM CORPUS pulled → ~/perkins-corpus/ (37 RoofR PDFs + OH calculators + pricing + manifest).

⛔ NEXT ACTIONS:
1. Marketing meeting 7/27 2pm EST — build a content/SEO status brief (375 articles, metal live,
   llms.txt/AIO, hub). Confirm agenda owner (DeGenito vs Wendy).
2. TIM-GATED estimator writes (prod-critical — confirm with Tim first): apply the daily_series
   overhead model from docs/ROOFR_OVERHEAD_TIERS.md (days=setup+rate*SQ), refresh PREFERRED
   adder $160→$165, run scripts/seed_gutters_config.py (gutters already built, 13 tests).
3. Get Wendy's actual style guide → then do #7 (settings page + site scan). BLOCKED without it.
4. Optional: fuzzy-backfill the ~52 keyword-mismatch video links; T&C/FAQ proposal toggle.
5. JON-GATED: prod cutover (staging→perkinsroofing.net); paid 3k article run.

DONE THIS SESSION (all pushed): articles QC + relativize + hub-relink (12966ff), lumber-chart
optional proposal attachment (738b638), estimator overhead-tier analysis (docs/ROOFR_OVERHEAD_TIERS.md).
Verified: #2 gutters already built, #4 warranty checker + page live, #5 metal live, #6 Greener done.

GOTCHAS: EMBED_BACKEND=vertex for grounding; parallelize with separate processes not --workers>1;
Cloud SQL proxy up; estimator config is the PAID product — don't write values without Tim.
R6: every commit updates docs + Jarvis + memory.

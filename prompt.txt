Resume perkins-roofing/video-archival.

READ FIRST: CONTINUATION-2026-07-27-pm.md, then docs/meetings/2026-07-27-marketing-and-estimator.md.

HEAD 18b75ac, pushed. Deployed platform:d44fb51 (the commits above it are docs only).
Prod configs jupiter v20 / miami v21 / naples v20. drift_check clean.
The Tim email went out 2026-07-27 16:08. He replied 3x (20:22, 20:24, 20:36) and the 2pm
meeting is transcribed. Work is in Jarvis #432-#445 under project perkins-buildout-2026-07.

Confirm:
  git log --oneline origin/main..HEAD                       # expect empty
  gcloud run services describe api --region us-central1 \
    --format='value(spec.template.spec.containers[0].image)' # expect platform:d44fb51

WHAT IS SETTLED — do not re-derive
  - 5-day week, 20 work days/month. Profit floor $2,500 x ceil(crew_days/5). $4,000 is dead:
    $2,500 at any size. All confirmed verbally on 7/27.
  - Miami office overhead = $85k/month / 20 = $4,250/day. Jupiter $28k/20 = $1,400 (already
    our config). Naples still unstated.
  - Accuracy: 83% within a day out-of-sample; validated against his ACTUAL prices at
    median +1.0%, 18/21 within 10%. He wants 95%+ on overhead.
  - Accessibility has NO fixed price — a MANUAL $ input, not a tier (his email 20:36).

THE WORK, ranked

1. #432 PROFIT MODEL. Tim: "that profit thing per square is an old thing... let's not have
   duplicate mechanisms. I would just eliminate it... $2,500 minimum and then use the sliding
   scale to figure out your percent." Replace the per-square band table with an operator
   PERCENTAGE + the $2,500 floor. Keep profit_scale readable so old proposals still render.
   This makes the "20 squares $120 or $110" question moot.

2. #433 CUSTOMER-VIEW LEAK, shipped this morning. Tim: "the PM incentive is not something the
   customer ever needs to see." calc_lines_from_estimate(audience="customer") folds
   base+overhead+profit but still prints PM Incentive as a row. Fold it, and add a test that
   no back-end key survives customer mode.

3. #434 REPAIR PROFIT. A repair quote returns COST only — he caught it live: 1 day, 1 man,
   $500 materials printed $1,685 ($1,185 + $500). Add the profit slider, $250 minimum profit,
   $500 minimum service call. Maintenance prices identically: relabel "Repair / Maintenance",
   ONE path (Josh wanted three; Tim and Jon overruled).

4. #437 FOUR INPUTS he asked for by email 20:24: accessibility input box (manual $),
   Resi/Commercial button, crane flag for >2.5 stories, waterfront/salinity gate for Coastal.
   Coastal is a FOURTH tier: good/better/best/coastal, on every roof type not just metal.

5. #436 REFIT THE DAY MODEL on Stories + Pitch + Accessibility — all three are now columns in
   ~/perkins-corpus/tim30_with_actual_prices.json — and measure against his 27 ACTUAL prices,
   not just his day figures. He offered another 20 houses if it helps.

6. #435 seed Miami $4,250/day. #438 TEST the salinity tool before wiring Coastal (built,
   converted to a WP plugin, never verified). #439 scrape TIM's Knowify catalog, not Josh's.
   #440 finish R2. #441 chase the 7 unanswered pricing questions.

GOTCHAS
- Excel ate Tim's pitch column as DATES: "4/12" is stored 2026-04-12. The MONTH is the rise.
- Transcribe locally for $0: ffmpeg -ac 1 -ar 16000 -b:a 32k -f segment -segment_time 600,
  then POST each chunk to http://cerberus-ai:4000/v1/audio/transcriptions model=whisper-turbo.
  Key: grep '^LITELLM_MASTER_KEY=' ~/litellm/providers.env. 69 min took ~4 min and cost $0.
- `pgrep -f "pytest tests/"` MATCHES YOUR OWN WAITER SHELL — an until-loop on it never exits.
  Wait on the PID. Same trap hit a deploy.sh waiter.
- A config change is FOUR places: fixture, prod, tests, any seeder that could replay it.
- Cloud SQL proxy 127.0.0.1:5432, db `perkins`, user `app`; every query needs
  set app.tenant_id='1'. PW=$(gcloud secrets versions access latest --secret=db-password)
- CI gates on `ruff check core adapters api jobs` FIRST. Full suite ~20 min, core/ ~97.7%
  (R1 >= 97%). .coveragerc fail_under=100 fails on a clean tree — pre-existing, not yours.
- Sweep ALL THREE corpora before asking Tim anything: transcripts, the 273 cell comments, and
  his ~92 emails (Graph $search="from:tim@perkinsroofing.net"; $filter+$orderby together 400s).
  The mailbox is the one that got missed and it cost two wrong pricing rules.
- Graph attachments are ADDITIVE — delete before re-attaching.
- Render a proposal and LOOK at it; reading the template is not the same.
- bash scripts/deploy.sh (not executable); refuses a dirty tree.

Read CONTINUATION-2026-08-12-pm.md first. It has the full state. Its §1 has the A/B numbers,
§3 has the R2 outcome and what remains, §4 is your task list, §5 has Jon's two open
questions (the calc-breakdown answer, and a UI bug that is diagnosed but NOT fixed).

CONTEXT IN ONE LINE: two features are BUILT, WIRED, COMMITTED and GREEN. R2 returned REJECT with two
CRITICALs (a filtergraph that broke every 2-region render, and an out-of-range time window that
redacted nothing while reporting success). BOTH ARE FIXED AND MUTATION-VERIFIED — see §3. What is
left is re-review of the fixes, the marking UI, and a UI bug — not rework of the features.

  Phase 1  core/audio_enhance.py    new `wind` option (highpass=f=90 + softer afftdn)
  Phase 2  core/video_redact.py     NEW — pixelates operator-marked PII regions
           plus wiring in core/render_spec.py, jobs/render_job.py, api/routes/clips.py

═══════════════════════════════════════════════════════════════════════════════
DO THESE IN ORDER
═══════════════════════════════════════════════════════════════════════════════

1. NOTHING TO VERIFY ON THE SUITE — it is green (5,702 passed, 5 skipped, exit 0 captured before
   any pipe) and everything through 566fccc is committed and pushed. Start at step 2.

   For later runs: capture the exit code to a file BEFORE any pipe. A task-notification saying
   "exit code 0" reports the WRAPPER's exit, not pytest's. And pyproject sets addopts="-q" — do
   NOT pass -q again or the summary line disappears and a green run looks truncated.

2. RE-RUN R2 on the POST-FIX code. The two CRITICALs are fixed but the FIXES have not themselves
   been reviewed. Pay attention to the `split`-based filtergraph and the new clip_duration guard.

3. DECIDE THE MARKING-UI QUESTION (§3, "Known-remaining").

   `grep -rn redact_regions` hits ONLY the five feature files. Nothing produces a region — there is
   no frame-scrub box tool, so no operator can confirm a box, which is the module's entire stated
   doctrine. Today the only path is a hand-computed PUT to /clips/{id}/render_spec.

   Either build the marking tool, or mark the field unreachable-by-design in render_spec.py so the
   next reader does not take it as shipped. Do not leave it ambiguous — "config with no reader
   reads as done" is a recorded failure mode in this repo, and this is its mirror image.

   NOTE: the question the last session worried about — whether reframe undoes the redaction — is
   ANSWERED and CLEAN. The reviewer traced the whole chain; nothing re-derives from src_path after
   the cut, and pixelation is destructive so a later crop cannot restore detail.

4. THE ESTIMATE-INPUTS UI BUG (§5B) — separate, small, and worth doing while you are here.
   Quoting.tsx:2473 uses `gridTemplateColumns: "1fr 1fr 1fr"`. `1fr` is `minmax(auto, 1fr)`, so a
   long unbreakable string ("50,373.4 ft to salt water (CORAL GABLES CANAL (EAST))") forces
   min-content width and squeezes its siblings — that is why "Include the Coastal package" wraps
   one word per line in Jon's screenshot.

   Likely fix is `minmax(0, 1fr)` x3 plus `minWidth: 0` on the child. CHECK IT IN A BROWSER, do
   not just edit the CSS and declare it fixed — the whole point of the screenshot is that it looks
   wrong, so the verification has to be visual. Lines :608 and :2446 carry the same pattern.

   Do this as its OWN commit, not folded into the render work.

5. COMMIT — two separate commits, Phase 1 then Phase 2. Trailer: `No-Task: <reason>` alone on its
   line, plus:
     Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

═══════════════════════════════════════════════════════════════════════════════
RULES
═══════════════════════════════════════════════════════════════════════════════

- DO NOT deploy.
- DO NOT touch the metal warranty page — awaiting Tim/Marco/Josh's reply.
- DO NOT release the 7 `held` scheduled_content rows — Jon's call.
- ./ab-review/ is gitignored demo media. Do not commit it. Do not delete it — Jon may not have
  reviewed it yet.
- Label every finding VERIFIED FROM DOCUMENT or INFERENCE. Do not merge the two. Five corrections
  in one session yesterday all came from stating an inference with the confidence of a fact.
- If Phase 2 turns out to be unsafe on re-review, SAY SO and stop rather than shipping it. A
  reasoned "this does not work yet" is worth more than a merged privacy feature that leaks.

DO NOT rebuild the "show me the formulas" feature — it already exists and §5A documents exactly
where it is and the two conditions (role `estimating_manage`, and rows frozen at create time). If
Jon reports the section missing from a PDF, check the ROLE before touching any code.

IF JON HAS ALREADY GIVEN A VERDICT ON THE A/B: apply it. If he said the wind difference is too
small to matter, the next lever is DeepFilterNet3 as an adapter behind the same `wind` flag — but
do not add that dependency without his go-ahead.

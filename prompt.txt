Resume the Perkins full-funnel platform. C1 Part 2 (strict=True) + #321 Contract-FAQ + #40
deepsec M/L are ALL BUILT + GATED + R2-REVIEWED + COMMITTED (a5b694d, 4caa493, 7afcf69) but
NOT DEPLOYED. Read IN ORDER first:

1. CONTINUATION-2026-07-10.md            — full handoff (state, DEPLOY RUNBOOK, gotchas)
2. Memory index (auto-loaded)            — esp. c1-deploy-blocked-gcloud (token-mint recipe)
3. docs/ENGINEERING_RULES.md             — R1–R5 binding
4. mcp__jarvis-memory__sync then my_tasks — live human blockers (Perkins v2 project)

PRIMARY TASK — SHIP IT (the only blocker was Google auth reauth, needs Jon):
  1. Jon: `! gcloud auth login` AND `! gcloud auth application-default login`.
  2. `git push origin main` if not pushed (CI = full gate + npm audit).
  3. Migration 0023 FIRST (contract-faq table), via scripts/apply_migrations_adc.py
     (MIN_MIGRATION=0023, ADC-only).
  4. `scripts/deploy.sh` (clean tree required), then firebase hosting deploy
     (`! firebase login --reauth`) for the Contract FAQ page.
  5. PROD SMOKE — the authoritative strict=True check (SQLite no-ops the event):
     /search /ask /status + a CRUD route + GET /contract-faq + run one ingest job;
     Cloud Logging must show NO "tenant_id not set on session.info". Token-mint recipe
     in memory c1-deploy-blocked-gcloud. Rollback = redeploy d9e2e5b or strict=False.

ALSO BUILDABLE (no external dep): #89 retrieval/eval harness (last "production v1" item).
GATED ON JON (don't attempt): H1 edge (Cloudflare token + LB / jarvis #330 DNS), Tim's
estimator pricing + clips + ToS, YouTube OAuth mint, deposit/ownership reconcile.

NON-NEGOTIABLE DISCIPLINE:
- TDD fail-first ALWAYS. Full gate before "done": ruff clean (`ruff check core adapters api jobs`)
  + `pytest tests/ --cov=core --cov-fail-under=100` (background to /tmp log + EXIT marker —
  headroom garbles inline pytest output) + `cd web && npm run build` for SPA changes.
- Implementation subagents = sonnet; opus for plan/architecture/security/final verification only.
  VERIFY agent worktree isolation actually happened (`git worktree list`) before concurrent work.
- Commit + push at each green milestone. R3: infra ONLY via terraform, never gcloud-by-hand.
- Do NOT relitigate locked decisions. Jon authorizes applies/deploys — still surface
  hard-to-reverse prod actions.

CRITICAL GOTCHAS (see continuation for detail): strict=True is LIVE in the code — any new
session must be stamped (get_db_session / for_each_tenant / tenant_id param) or
platform_scope; SQLite no-ops the event so grep+trace is the only pre-prod detector;
RLS GUC needs NULLIF(current_setting(n,true),'') on pooled connections; migrations can't
DML RLS-forced tables under the app role; create_all now auto-applies RLS to tenant tables
(after_create hook in app/models.py); get_db_session 403s without tenant context.

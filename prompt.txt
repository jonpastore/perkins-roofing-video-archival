Resume perkins-roofing/video-archival.

READ FIRST: CONTINUATION-2026-07-30.md — §0 is the whole session. Then read BOTH adversarial
reviews in full before touching overhead:
    ~/perkins-corpus/ai-reviews/grok_review.txt   (the sharper one)
    ~/perkins-corpus/ai-reviews/gpt_review.txt
    ~/perkins-corpus/ai-reviews/brief.md          (exactly what they were asked)

HEAD e97686b, pushed, CI green. Deployed platform:60a06a9 (e97686b is docs-only).
Prod migrations applied through 0051.

Confirm before trusting any of this:
  git log --oneline -1 && git status --short
  git log --oneline origin/main..HEAD          # expect empty
  gcloud run services describe api --region us-central1 \
    --format='value(spec.template.spec.containers[0].image)'
  bash scripts/plan_as_ci.sh                   # expect "plan exit code: 0"
  gh run list --limit 4                        # MATCH THE SHA; "deploy skipped" == nothing shipped

⛔ STAY ON STAGING. PlatformConfig.WP_URL = https://1228404.us6.myftpupload.com is CORRECT and
intended. Do not vault a prod WP app password, do not publish to perkinsroofing.net. The prod
cutover is a parked BUSINESS gate — "verified live == staging" throughout the docs is right.

⛔ DO NOT CHANGE PRICING. overhead_basis is 'branch' on all three branches with a flat $1,400/day.
Nothing about the overhead model is settled. Two independent reviews say the analysis that
looked settled was circular. Read them first.

▶ START HERE — THE OVERHEAD MODEL, WITH THE REVIEWS' FINDINGS ALREADY APPLIED

  Jon's direction: "the sheets have his old formula for per sq by roof type but he uses PER DAY
  BY ROOF TYPE and that's what we need to achieve." Tim confirms the sheets are legacy.
  ⚠️ This contradicts two earlier instructions in the same conversation ("go with uniform
  pricing", and earlier "not make it uniform"). Re-confirm with Jon before implementing.

  FOUR THINGS THE REVIEWS ESTABLISHED — all re-verified numerically, do not re-derive:

  1. "The four rates = crew size x per-man-day" is FALSE for installs. If it were true the
     demo/install ratio would be 5/3 = 1.667. Actual: demo/tile 1.409, demo/shingle 1.500,
     demo/metal 1.235. It holds for demo alone, coincidentally.
     ⚠️ The DRAFT EMAIL TO TIM STILL CONTAINS THIS REFUTED CLAIM. Fix before sending.

  2. Tim's own rates UNDER-RECOVER his own burn. Under his stated 1.5 crews:
        tile    0.5x1050 + 745 = $1,270 vs $1,470  -> -$200/day (-13.6%)
        shingle 0.5x1050 + 700 = $1,225            -> -$245/day (-16.7%)
        metal   0.5x1050 + 850 = $1,375            ->  -$95/day  (-6.5%)
     His four numbers and his $1,470 cannot both be right. Ask him to DERIVE $745.

  3. PARALLEL-JOB DOUBLE COUNT — the largest conceptual hole, missed entirely. Branch burn is
     per CALENDAR-day, not per job-day. At his 1.5 concurrent jobs, charging each job a full day
     collects 1.5 x $1,470 = $2,205 against a $1,470 office: +50% over-recovery. The estimator
     has no rule for this. Define it before choosing any rate model.

  4. Productivity error dwarfs the rate debate. Metal sheet 5.5 sq/day vs actual 8.0 = 45% error
     in days and therefore in overhead. The whole rate argument moves metal ~3%.

  THE ONLY NON-CIRCULAR TEST, NEVER RUN:
     recovery identity = SUM(OH charged over a month) / (1,470 x working days) ~= 1.0
  Uses accounting burn + estimator-charged days, independent of sold prices. Run this first.

  ⚠️ STOP VALIDATING AGAINST SOLD MEDIANS. Sold prices embed Tim's own overhead, so a match
  proves only "our OH + our profit ~= his OH + his profit" — passable with wrong OH and
  compensating profit. The "+0.8% metal" figure is n=3 homes at 2024 prices vs a 2026 benchmark;
  both reviewers say withdraw it. Grok flagged motivated reasoning: the table makes $210/man look
  best and $210 is the config's pre-existing assumption.

▶ THEN — MIAMI. BOTH REVIEWERS SAY THIS SHOULD HAVE LED.

  Miami burns $4,257/day (2.9x Jupiter) and is priced at a flat $1,400/day with JUPITER'S
  IDENTICAL four roof-type rates. It recovers ~1/2.9 of its office. Tim: Miami "has literally
  lost money or broken even every quarter since Q2 2024."
  ⚠️ Do NOT fix by scaling Jupiter's four rates by 2.9 — that preserves the folklore ratios and
  multiplies them. Interim: flat $4,257 x days. Tim will not share the Miami OH sheet (salaries).

WHAT IS SETTLED — do not re-derive
  - COST AND MATERIALS ARE EXACT. All four sheet quadrants verified line by line at 25 SQ:
    FBC sloped (5 types) 0.0%, HVHZ sloped (5 types) 0.0%, low-slope (8 systems) 0.0%, on BOTH
    base cost and overhead. His own worked example: his sheet $18,625, ours $18,475 (-0.8%),
    and the -0.8% decomposes fully (his sheet contradicts itself on shingle OH: the worked
    example says $125/sq, his own OH table on the same tab says $105).
  - PII IS A HARD GATE and now covers PIXELS. CompanyCam burns capture GPS into the image
    (25.858694 N 80.120019 W). core/pii.py is text-only and returned no_pii:true on a gallery
    that would have published a client's building. core/photo_privacy.py cuts the bottom 20%
    and media_sanitized requires every published img/video to be WP-hosted. Nothing was ever
    exposed. Do NOT loosen without re-running the 26,063-line corpus check.
  - gate_failures (migration 0051, APPLIED) persists WHY a project or article was refused.
    NULL = never gated, [] = gated and clean. They are different; code that conflates them
    reports unchecked items as passing.
  - The JSON-LD plugin is FIXED and UPLOADED. Staging runs the REGULAR plugin (v1.3.0 active),
    NOT a mu-plugin — installing the mu-plugin alongside it duplicates schema on ~60 articles.
  - Tim's Jupiter headcount is LOGGED, not assumed: 157 days, mean 6.17, median 6
    ("2026 OH Average" sheet, Jupiter tab). Exclude the 968 totals row.

STILL OPEN
  - overhead_basis unchanged; office_daily_overhead stale (config 1400/4250 vs Tim's 1470/4257);
    naples carries Jupiter's 1400 and office_men = None.
  - Tile prices +10-12% under EVERY overhead model. Not an overhead problem. Unexplained.
  - Email to Tim DRAFTED NOT SENT in jon@degenito.ai (subject "The estimator now reproduces your
    sheets to the dollar..."). Must be corrected per §0.1 before sending.
  - Portfolio blocked on permission_property (Tim never answered). 8 of 13 projects also have
    ZERO Knowify scope; only 5 are curatable.
  - TPO sold history unusable — work-type contamination (maintenance at $65/sq mixed with a real
    re-roof at $1,796/sq), NOT a unit bug. Tile/shingle/metal are clean.
  - o365 MCP refresh token expired (AADSTS700082, verified) — use gmail-enhanced for that mailbox.
  - The wp-admin login password sat world-readable in /tmp for a week; shredded, NOT rotated.

GOTCHAS
- ⚠️ pgrep -f "<pattern>" MATCHES THE POLLING SHELL'S OWN COMMAND LINE. Six waiters deadlocked
  this way in one session. Poll a status FILE or use kill -0 $PID. Same family as pkill -f.
- ⚠️ The session scratchpad is WIPED ON RESTART. Two long AI reviews and every analysis script
  were lost mid-flight. Durable work -> ~/perkins-corpus/. Long external jobs -> setsid nohup.
- ⚠️ NEVER edit the tree while a long gate runs (R7): coverage maps line numbers from the file on
  disk at report time. Three 40-60 min runs were invalidated this way in one session.
- ⚠️ `cmd | tail -n` returns TAIL's exit code — a failing suite reports success.
- ⚠️ Console output is truncated. Never copy an identifier out of a printed table.
- Migrations are MANUAL: MIN_MIGRATION=0051 .venv/bin/python scripts/apply_migrations_connector.py
  (it strips comments before splitting on ';' — do not hand-roll that).
- Pre-push tests: .venv/bin/python -m pytest tests/api tests/core tests/adapters tests/jobs
  tests/tenancy  = ~4,099 tests in ~8m. Full tests/ with coverage is 40-60 min on this box.
- CI gates `ruff check core adapters api jobs` FIRST, then pytest --cov-fail-under=97 (97.86%).
  SPA: cd web && npx tsc -b.
- Cloud SQL proxy 127.0.0.1:5432, db perkins, user app; every query needs set app.tenant_id='1'.
  PW=$(gcloud secrets versions access latest --secret=db-password).
- Tim's sheets read via perkins-deploy SA + DWD impersonating tim@perkinsroofing.net.
  sloped 1qxfKRRvmQS_NYu3AE2KQgek421Wzftu3xVmGECFH-ig (OH Metrics tab = MIAMI)
  low-slope 1hTGWCWzIVLgWwNFln_AYBnEcKkj0tLbaZiv82zHXWWQ
  Jupiter OH 1V3uGnb57oo5Kh8IZk7fJSsjjakAmdVeejPisAIouJGc
- EMAIL_SEND_MODE=test in prod; only jpastore79@gmail.com + @degenito.ai + @perkinsroofing.net
  receive mail. Do not flip to live without Jon.
- R7-R10 are new in docs/ENGINEERING_RULES.md and are binding.

Resume perkins-roofing/video-archival.

READ FIRST: CONTINUATION-2026-07-30-pm.md — §0 lists what the MORNING doc (CONTINUATION-2026-07-30.md)
got wrong, so read the pm one and treat the am one as superseded. Then, before touching the
warranty tool:
    docs/superpowers/specs/2026-07-30-brackish-confidence.md    (what to build)
    docs/superpowers/plans/2026-07-30-brackish-confidence-plan.md (W1/W2/W3, gates)
    docs/BRACKISH_DATA_SOURCES.md                               (the measured evidence)
    docs/WARRANTY_TOOL_TIDAL_LAYER.md                           (what already shipped + why)

HEAD 5afb0d2, pushed, tree clean. Deployed platform:a86d941 (later commits are docs/scripts/plugin
only). Warranty plugin 1.1.2 live on staging. Prod migrations through 0051.

Confirm before trusting any of this:
  git log --oneline -1 && git status --short
  git log --oneline origin/main..HEAD          # expect empty
  gcloud run services describe api --region us-central1 \
    --format='value(spec.template.spec.containers[0].image)'
  bash scripts/plan_as_ci.sh                   # expect "plan exit code: 0"
  gh run list --limit 4                        # MATCH THE SHA; "deploy skipped" == nothing shipped

⛔ STAY ON STAGING. PlatformConfig.WP_URL = https://1228404.us6.myftpupload.com is CORRECT and
intended. Do not vault a prod WP app password, do not publish to perkinsroofing.net.

⛔ DO NOT CHANGE PRICING without Jon. Live: jupiter + naples on overhead_basis="series" (flipped
today, Jon's call); miami on "branch" at $4,250/day; concurrent_crews deployed but UNSET everywhere.

▶ START HERE — W1 GAUGE ANCHORING (spec + plan already written, nothing built)

  Snap the 64 USGS salinity gauges to reaches, propagate each reading along channel distance
  stopping at control structures (cap ~2 mi), and let MEASUREMENT override both the OSM tag and
  connectivity IN BOTH DIRECTIONS. It must be able to REMOVE a warning, not only add one.

  The two cases that prove it:
    Loxahatchee at US-1, Jupiter   55,100 uS/cm (seawater) — we currently call it a guess
    Upstream Broad River            460 uS/cm (fresh) but OSM-TAGGED — a measured false positive
                                    sitting in the only bucket that moves a verdict

  Gate: scripts/validate_tidal_against_gauges.py agreement rises from 80%, scored HOLD-ONE-OUT
  (fit on n-1, score the held-out gauge) — in-sample scoring is the same circularity trap the
  overhead analysis fell into. The three never-tagged pins in check_tidal_layer.py must still pass.

  DO NOT re-derive these — all queried live 2026-07-30:
    - USGS NWIS param 00095, waterservices.usgs.gov, free + keyless, 64 gauges in bbox, 64 live.
    - SFWMD publishes Saltwater_Interface (250 mg/L isochlor, 2024, 7 polylines, 130 KB),
      ChlorideConcentrationControlPoints, and DBHYDRO (1,673 active WQ stations).
      ⚠️ the isochlor is GROUNDWATER, not surface canals — corroborating, not a substitute.
    - Structures ARE the salt line, confirmed by instruments: two gauges on the SAME C-8 canal read
      473 and 29,900 uS/cm across structure S-28.

▶ THEN W2 — THE LEARNING CACHE (Jon's idea, and the reason this scales)

  A check that lands "inferred"/"none" within the mile that matters records a research request; a
  scheduled job resolves it (USGS + DBHYDRO + isochlor + targeted Overpass re-query for unmapped
  structures) and stores a determination with provenance + expiry; the nightly rebuild folds it in.
  ⚠️ The plugin STAYS STATIC — determinations feed the BUILD, not a runtime call.
  ⚠️ PRIVACY IS A HARD GATE: store reach id + a coarse ~1 km cell. NEVER the address, never the
     exact coordinate. Today's CompanyCam finding was publishing a client's building at 0.1 m.

▶ W3 — PER-PHASE STATUS. The tool prints one spinner and, until this week, hung silently forever.
  ⚠️ ASK JON FIRST: does "each of the rendering actions" also mean the SPA's long-running
  proposal-render / publish / batch-article actions? Currently scoped to the warranty tool only.

WHAT IS SETTLED — do not re-derive
  - TIM'S OWN SHEET ANSWERS THE CREW QUESTION. His Jupiter tab = (office/day / men) x crew /
    squares-per-day at $200/man-day; its "Re-Roof OH (7 men)" line is $185.72 / $105 / $204.75 =
    his published FBC table to the dollar. So his per-square table ALREADY splits the office by
    crew: a 3-man crew carries 3/7 = $600, not $1,400. His 7/24 rates sit 5-42% ABOVE that share
    and that gap is the ONE number not derivable from anything he has sent.
  - Cost and materials match his live sheets EXACTLY — re-verified cell by cell 7/30 via
    scripts/verify_config_against_tim_sheets.py (all 20 sloped numbers, all 8 low-slope systems).
    FBC Polyglass $450 is his own "-$25 FBC (no FR)" note, NOT a discrepancy.
  - "One model with one knob" FAILS (scripts/prove_persq_day_equivalence.py). Ship both modes.
  - Recovery identity: Jupiter 465.5 charged job-days / 392 working days = 1.19 (a floor).
  - The Knowify mirror is the MIAMI tenant; Jupiter is a separate tenant reachable only via the
    Knowify MCP. BusinessState="Open" is the acceptance marker, NOT IsSigned (5% of contracts).

STILL OPEN
  - Tim email DRAFTED NOT SENT (jon@degenito.ai, "Your sheet answers the crew question — plus 7
    things I need"); copy at ~/perkins-corpus/tim_email_2026-07-30.txt.
  - Miami charges its whole office day per job -> ~$2,087/sq HVHZ tile vs $1,113/sq accepted.
    concurrent_crews:4 -> $1,343/sq, but 4 is Tim's capacity target, not a measurement.
  - Crew share (-0.3%) vs his four rates (+2.2%): one config write once he answers.
  - naples carries Jupiter's 1400 + office_men=None; office_daily_overhead stale vs 1470/4257.
  - Tile +10-12% over sold under EVERY overhead model. Unexplained, predates today.
  - The Maps browser key (perkins-setback-widget) is NOT Terraform-managed — console state.

GOTCHAS
- ⚠️ Overpass answers HTTP 406 to a bare POST body — form-encode the query as data=.
- ⚠️ SFWMD's ArcGIS host 403s a default curl UA. Send a browser UA.
- ⚠️ A referrer-rejected Google Maps key returns HTTP 200 and reports via gm_authFailure; onerror
  never fires, so a promise waiting on the callback hangs forever with no error shown.
- ⚠️ String ids + PYTHONHASHSEED = a non-reproducible build. Sort seeds AND output.
- ⚠️ A regression pin that measures the wrong thing passes forever — ours read the globally nearest
  segment when inferred outnumbers tagged 22:1. Assert on the bucket that actually moves verdicts.
- ⚠️ `sleep N` chained after another command is blocked; use an until-loop or run_in_background.
- ⚠️ pgrep -f "<pattern>" MATCHES THE POLLING SHELL'S OWN COMMAND LINE. Poll a status FILE.
- ⚠️ The session scratchpad is WIPED ON RESTART. Durable work -> ~/perkins-corpus/.
- ⚠️ NEVER edit the tree while a long gate runs (R7).
- ⚠️ `cmd | tail -n` returns TAIL's exit code — a failing suite reports success.
- Pre-push tests: .venv/bin/python -m pytest tests/api tests/core tests/adapters tests/jobs
  tests/tenancy  = ~4,099 tests in ~8m. The summary line may not flush to a redirected log; the
  EXIT CODE is the evidence.
- CI gates `ruff check core adapters api jobs` FIRST, then pytest --cov-fail-under=97.
- Cloud SQL proxy: ~/bin/cloud-sql-proxy (fetched 7/30; /tmp copies get wiped). Start with
  `setsid nohup ~/bin/cloud-sql-proxy video-archival-and-content-gen:us-central1:video-archival-and-content-gen-pg --port 5432 &`
  PW=$(gcloud secrets versions access latest --secret=db-password); every query needs
  set app.tenant_id='1'.
- WP plugin upload: bump PERKINS_MWC_VERSION, zip the folder, then
  `set -a; source .env; set +a; export WP_LOGIN_PW="$WP_PWD";
   PYTHONPATH=. .venv/bin/python scripts/wp_install_plugin.py <zip>`  (WEB login, not the REST app pw)
- Tim's sheets via perkins-deploy SA + DWD impersonating tim@perkinsroofing.net.
  sloped 1qxfKRRvmQS_NYu3AE2KQgek421Wzftu3xVmGECFH-ig (tabs incl. OH Metrics = MIAMI, Jupiter)
  low-slope 1hTGWCWzIVLgWwNFln_AYBnEcKkj0tLbaZiv82zHXWWQ
  ⚠️ Cell COMMENTS carry values the visible cells do not (FBC TPO OH $125; the busy-season tiers).
  Comments are a DRIVE resource, invisible to Sheets values().get.
- EMAIL_SEND_MODE=test in prod. R7-R10 in docs/ENGINEERING_RULES.md are binding.

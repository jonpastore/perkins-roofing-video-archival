Resume perkins-roofing/video-archival after a restart.

READ FIRST: CONTINUATION-2026-07-25.md (full state, mistakes, next actions).
HEAD == b1a4277 (+ the docs commit that adds the continuation). Deployed image platform:b390ba6 —
commits after it are docs/config-only; confirm with:
  git diff --name-only b390ba6..HEAD | grep -v '^docs/'
Prod pricing configs are at v13 on jupiter/miami/naples.

WHERE THINGS STAND (2026-07-25)

- ESTIMATOR NOW RUNS TIM'S METHOD. Days come from RoofR cut geometry (not squares), plus +0.5
  install days at pitch >= 6/12. Against Tim's own day figures on 29 of his 30 homes:
  93% within 1.0 day, 66% within 0.5, mean miss 0.53 days, mean overhead within $3/home.
  Reproduce: DB_URL=... PYTHONPATH=. .venv/bin/python scripts/tim_quote_breakdown.py
  Refit:     scripts/fit_days_from_roofr.py   (leave-one-out, non-negative coefficients)

- MONEY BUG, PARTLY FIXED. The flat published catalog under-quotes Tim's own build on 30/30 homes
  (mean -$72/sq, worst -$150/sq). Tile full prices are now blocked from the catalog
  (requires_engine_price -> 422). Shingle/metal still catalog-priceable and still under-quoting.
  ⛔ JON/TIM DECISION: retire the flat catalog as a price, or re-issue it size-banded.

- UNBLOCKED: Tim's 77 sheet cell COMMENTS are readable via the Drive API + existing DWD grant
  (7/17 item #9 assumed we had to ask him). Saved to ~/perkins-corpus/tim_sheet_comments.json.
  They carry the material->price breakdowns, L/M/OH/P decompositions, the 7/12 adder (OH $90/sq)
  and crew-size variants ("busy, 16+ guys" / "huge job").

⛔ NEXT ACTIONS
1. SEND TIM THE EMAIL — docs/email-drafts/2026-07-24-tim-estimator-quotes.md is a DRAFT, not sent.
   It asks: why our quote beats his published sheet on all 29 homes, what differs on the four
   1.5-day homes (esp. 1913 Flower Dr vs 918 Mil Creek — identical on paper, 9.5 vs 8.0 days),
   the NOTES COLUMN he promised, 1141 Vintner Blvd's missing RoofR report, a few 7/12+ roofs, and
   9 config values only he can fill.
2. Decide the catalog question above.
3. Build the price-book <-> aggregate-input linkage from the 77 comments (7/17 item #9's real work).
4. BigQuery billing export so the GCP spend panel lights up: docs/BILLING_EXPORT_SETUP.md, then
   BILLING_BQ_TABLE into deploy.sh BASE_ENV and redeploy.
5. Not started: metal-adjacent article enumeration among the 262 scheduled; article
   image-vs-YouTube-title-screen audit.
6. Marketing brief for 7/27 — recommended DROPPED (that meeting is drip messaging / competitors /
   appointment forms per Tim's 7/24 thread, not a content-SEO review). Jon to confirm.

STILL PENDING TIM: 9 config values + 5 _pending notes — see CONTINUATION §4. His own sheet does
NOT answer them (3-5 storey and 6+ crane adders are blank in his sheet too). PM incentive is the
notable one: values match, KEYING does not, so a 35 SQ residential job takes $50 where his sheet
says $100.

GOTCHAS
- EMBED_BACKEND=vertex and export GOOGLE_APPLICATION_CREDENTIALS="$(scripts/fetch_vertex_sa.sh)"
  for anything retrieval-shaped; the .env default path does not exist.
- Cloud SQL proxy on 127.0.0.1:5432; PW=$(gcloud secrets versions access latest --secret=db-password)
- bash scripts/deploy.sh (not executable); refuses a dirty tree (R3-ENFORCE).
- articles is keyed by slug — there is no id column.
- Outbound mail is EMAIL_SEND_MODE=test; nothing reaches Tim without Jon flipping it.
- CI gates on `ruff check core adapters api jobs` FIRST — a lint error there silently skips
  bandit/pip-audit/coverage. It hid a red build for a full day.
- Verify features end-to-end in PROD, not just with unit tests: the geometry model shipped inert
  once because the route withheld its inputs, and the eaves model broke once because a caller
  omitted eaves_lf. See CONTINUATION §7.

R6: every commit updates docs + Jarvis + memory.

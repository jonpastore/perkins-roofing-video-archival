Resume perkins-roofing/video-archival.

READ FIRST:
    CONTINUATION-2026-08-01-pm.md     LAST NIGHT — migration runner fixed, #430 slice 2 built
    CONTINUATION-2026-08-01.md        the bid-project work it follows
    CONTINUATION-2026-07-31-pm.md     warranty tool statewide + the OAuth account-scoping fix

⚠️ NOTHING FROM LAST NIGHT IS COMMITTED. Tree is dirty on purpose — review before you push.
⚠️ NOTHING IS DEPLOYED. Migration 0052 IS NOW APPLIED (it was not before).

Confirm before trusting any of this:
  git status --short                           # expect the slice-2 + portfolio changes, uncommitted
  .venv/bin/ruff check core adapters api jobs  # CI's FIRST gate
  bash scripts/plan_as_ci.sh                   # expect "plan exit code: 0"
  .venv/bin/python -m pytest tests/api tests/core tests/adapters tests/jobs tests/tenancy
    # ~8m. THE EXIT CODE IS THE EVIDENCE — the summary may not flush to a redirected log.

⛔ STAY ON STAGING. PlatformConfig.WP_URL = https://1228404.us6.myftpupload.com is CORRECT.
⛔ DO NOT CHANGE PRICING without Jon. jupiter+naples overhead_basis="series"; miami "branch"
   at $4,250/day; concurrent_crews deployed but UNSET everywhere.
⛔ DO NOT send client email. EMAIL_SEND_MODE=test and Jon sends it.

════════════════════════════════════════════════════════════════════════════════
⚠️ THE MIGRATION RUNNER: READ THIS BEFORE ADDING ANY MIGRATION
════════════════════════════════════════════════════════════════════════════════
scripts/apply_migrations_adc.py IGNORES DB_URL — it always targets PROD via the Cloud SQL
Connector from GOOGLE_CLOUD_PROJECT. Older docs show a `DB_URL=postgresql+psycopg://... python
scripts/apply_migrations_adc.py` incantation; the prefix is decorative and does nothing.

There is NO applied-ledger. Every run re-executes EVERY migration from 0013 forward and relies
entirely on each being idempotent. One unguarded statement aborts the run and SILENTLY BLOCKS
every later migration — that is exactly how 0041-0052 sat unapplicable behind 0040's bare
ADD CONSTRAINT. Three such defects were fixed last night (0029 GUC, 0040 constraint, the
single-quote-blind splitter). AFTER ADDING A MIGRATION, RUN THE SCRIPT AND CONFIRM IT REACHES
"DONE".

⚠️ 0026 and 0027 carry UPDATEs with no guard and re-assert their values on every run. Harmless
only because tenant 2 was never onboarded and Ez-Bids W2 never re-scoped cors_origins. Onboard a
second tenant and the next migration run silently reverts it.

  .venv/bin/python scripts/apply_migrations_adc.py     # no DB_URL; it goes to prod

▶ START HERE — ONE OF THESE, ASK JON WHICH

  1. REVIEW + COMMIT last night's work, then deploy. deploy.sh does NOT run migrations and 0052
     is already applied, so the ORM is safe to ship. R2 (architect + critic deep review) has NOT
     been run on slice 2.

  2. SLICE 3 — proposal surface. Proposal.bid_project_id now EXISTS, so the gate is testable.
     ⚠️ web/src/pages/Proposals.tsx:414-419 re-quotes exactly ONE estimate and overwrites
     quote_snapshot. On a project snapshot that SILENTLY DESTROYS EIGHT BUILDINGS. Also
     api/routes/proposals.py:143-167 _assemble_review_text reads ONE roof, so the LLM send-gate
     would review one building and green-light a nine-building contract.

  3. SEND TIM THE SIX QUESTIONS — drafted at docs/2026-08-01-six-questions-for-tim.md. All six
     survived a full corpus sweep (zero hits each). Q3 is worth $8-16k on Evergrene alone.

  4. CURATE THE FIVE READY PROJECTS in the admin UI (Jon does this): isola, olsen,
     fisher-island-7900, fisher-77, pinnacle. Pick photos AND type alt text per image.

  5. IAM: api-run-sa cannot create a secret, so the OAuth connect flow 502s. Needs a Terraform
     grant — a security-scope decision, deliberately left for Jon.

DO NOT RE-DERIVE (all measured, all verified last night)
  - Evergrene still scores ours $390,230 vs Tim $381,288 (+2.3%), profit $30,790 vs $30,363
    (+1.4%) AFTER the mapping refactor. Re-run: scripts/validate_against_evergrene.py
  - PERMISSION IS UPSTREAM OF ALL PORTFOLIO MEDIA. _available_media filters the mirror through
    publishable_media, so with permission_photos false a project reports ZERO photos against a
    156,203-photo mirror. All 13 read "no media" purely because no flag was set. Now recorded
    (scripts/portfolio_grant_permissions.py, idempotent, provenance in updated_by).
  - Wendy chose the AVADA PORTFOLIO system; the nine /portfolio/ pages are being retired. Our
    publisher already targets the right type. She did NOT answer schema/JSON-LD/alt/links.
  - Tim: every project has client permission (contract term); location = town/city/HOA, never
    the street address. ⚠️ That is about MARKETING PAGES and does NOT answer whether a PROPOSAL
    needs each building's address.
  - alt text is typed PER PHOTO by an editor (gallery_html reads sel["alt"]) — alt_present /
    alt_unique can never be satisfied by code.
  - #449's spec still does not reconcile: "per site per WEEK" is 17 x $2,500 = $42,500 against
    Tim's actual $30,363. Default basis stays 'project'; both numbers returned every call.

STILL OPEN
  - R2 deep review never run on slice 2 (ENGINEERING_RULES R2 requires architect + critic).
  - jim-malooly-delray-beach-roof trips title_not_a_person (rename); abacoa + miami-warehouse
    have NO CompanyCam url; 6 projects match no Knowify scope; 4 write-ups under 120 words.
  - Miami charges its whole office day per job -> ~$2,087/sq HVHZ tile vs $1,113/sq accepted.
  - #447(3) commission_pct keyed by slope type, not per salesperson as PRICING_RULES §11 says.
  - #444 GCP budget: Billing API disabled, SA cannot enable it.
  - REACH_MI residual: 8 of 18 far live gauges never snap to mapped water.
  - Salinity banking is correct and INERT (every latest-only gauge is also stale).

GOTCHAS
- ⚠️ app/models.py carries 18 PRE-EXISTING ruff E702 errors (lines 63-233). app/ is NOT in the CI
  ruff gate (`ruff check core adapters api jobs`). They are not yours; do not "fix" them blind.
- ⚠️ tile_dumpster_count is a ceil() — anything calling it per building over-counts.
- ⚠️ profit_guidance is ABSENT from the estimate result unless it is a daily-overhead quote.
- ⚠️ pricing_configs columns are `config` and `is_active`, NOT config_json/active. Roof types:
  13_tile | barrel_tile | 3tab_shingle | dimensional_shingle | standing_seam_metal.
- ⚠️ Do NOT `source .env` before GCS work: it sets GOOGLE_APPLICATION_CREDENTIALS to
  ./infra/vertex-dev-sa.json, A FILE THAT DOES NOT EXIST. Use
  $HOME/.config/gcloud/perkins-deploy-sa.json.
- ⚠️ DB_URL in .env is sqlite. App code needs postgresql+psycopg://... over the Cloud SQL proxy
  (`+psycopg` — psycopg2 is NOT installed). Sessions must set db.info["tenant_id"] or
  ["platform_scope"]; core/tenant.py refuses to query otherwise.
- ⚠️ resolved_wp_url() swallows every exception and returns "". EMPTY MEANS A CONFIG ERROR —
  usually that you left DB_URL on sqlite.
- ⚠️ .dockerignore ALLOWLISTS scripts/. A job importing a NEW script needs !scripts/<name>.py.
- ⚠️ The job container has NO writable $HOME. Path.home() raises PermissionError '/home/appuser'.
- ⚠️ GREEN CI + CLEAN TERRAFORM + A FIRING SCHEDULER PROVED NOTHING, TWICE. Verify a job by
  RUNNING it and reading its stdout.
- ⚠️ OSM `tidal=yes` means water LEVEL, not salinity. The St Johns is tidal ~160 mi inland.
- ⚠️ qwen3.6 passed all three review areas as "Correct" and was wrong twice. Local review is a
  second opinion, NEVER a gate. Security-critical review stays on Claude.
- ⚠️ A bare `until` spin with no sleep burns the whole timeout; `sleep N` chained after a command
  is blocked by the harness. Use Bash run_in_background with an until-loop.
- ⚠️ Printing .env leaks WP_PWD (it reached a transcript 7/31) — worth rotating.
- Cloud SQL proxy: setsid nohup ~/bin/cloud-sql-proxy \
    video-archival-and-content-gen:us-central1:video-archival-and-content-gen-pg --port 5432 &
- Terraform without the reauth invalid_rapt demands:
    cd infra && unset GOOGLE_IMPERSONATE_SERVICE_ACCOUNT
    export GOOGLE_APPLICATION_CREDENTIALS=$HOME/.config/gcloud/perkins-deploy-sa.json
    export USER_PROJECT_OVERRIDE=true GOOGLE_CLOUD_QUOTA_PROJECT=video-archival-and-content-gen
    export TF_VAR_cloudflare_api_token="$(gcloud secrets versions access latest --secret=cloudflare-api-token 2>/dev/null)"
- Tim's sheets via perkins-deploy SA + DWD impersonating tim@perkinsroofing.net. Cell COMMENTS
  carry values the visible cells do not, and comments are a DRIVE resource.
- Mail: the o365 MCP refresh token EXPIRED (inactive 90d). gmail-enhanced still reaches
  jon@degenito.ai; the jon@perkinsroofing.net Gmail token is ALSO expired and needs re-auth.
- EMAIL_SEND_MODE=test in prod. R7-R10 in docs/ENGINEERING_RULES.md are binding.

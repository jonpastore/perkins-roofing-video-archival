Resume perkins-roofing/video-archival after a restart (config changes for token optimization).

READ FIRST: CONTINUATION-2026-07-24.md (full state). HEAD == 4a01cd7 (clean, pushed).
SCRATCH (this run's plans/reports/logs): /tmp/claude-1000/-home-jon-projects-perkins-roofing-video-archival/fc25f81c-006d-444d-a7cf-26ac94fa2737/scratchpad
  (scratch is per-session; if it's gone, the durable state is the DB + git. Re-derive plans if needed.)

⛔ FIRST THING — a 100-article generation run may still be in flight:
  pgrep -af '[j]obs.batch_article_job'      # 5 detached procs (systemd PPID 1508), Vertex inference
  for i in 0 1 2 3 4; do echo -n "s$i: "; grep -c generate_scored_article $SCRATCH/run100_s$i.log; done
  It's a LOCAL orchestrator on morpheus calling Vertex (Gemini 2.5 Flash), publishing to STAGING WP
  (1228404.us6.myftpupload.com). Prod perkinsroofing.net is untouched (Jon-gated). Let it finish.

DONE TODAY (all pushed): the compliance gate is now FULLY DETERMINISTIC — closed every seo_ranking/
answer_first flake (bb843d1, 35e776b, 17bc445); reprocessed the existing 66 pre-gate articles to
100% on staging (scripts/reprocess_articles.py, 89b6950 — whole DB 113/113 compliant, all 61
published posts confirmed live-updated); added ONE grounding critic per generation iteration
(9ba04a9, fixed a Vertex response_schema bug); built scripts/validate_run_with_critic.py (4a01cd7).

⛔ OPEN DECISION (do NOT skip): the grounding critic is AGGRESSIVE — smoke-test flagged 1 article
with 20 blockers incl. general explanatory prose, not just fabricated specifics. DO NOT run
validate_run_with_critic.py --apply until the critic severity/prompt is calibrated to treat only
truly-invented specifics (prices/codes/measures absent from the transcript) as blocking. See the
CONTINUATION "OPEN DECISION" section.

NEXT ACTIONS (in order): (1) confirm the 100-run finished (reports in $SCRATCH/run100_s*_report.json);
(2) verify whole-DB compliance (scripts/reprocess_articles.py dry = lists any non-compliant);
(3) calibrate the grounding critic before any regen; (4) fix valid_video_ids blockers (only embed
grounded video ids) for a literal 100/100; (5) then Jon-gated: validate_run_with_critic --apply,
prod cutover, paid 3k run.

ENV RECIPE + gotchas: see CONTINUATION-2026-07-24.md "HOW TO VALIDATE". EMBED_BACKEND=vertex required;
one process per --workers 1 (parallelize with SEPARATE processes, not --workers>1 which wedges);
Cloud SQL proxy already up. R6: every commit updates docs + Jarvis + memory.

Resume perkins-roofing/video-archival.

READ FIRST:
    CONTINUATION-2026-08-11.md         ← START HERE. §0 is the pattern; §5 is the plan; §3 is blocked-on-Jon.
    CONTINUATION-2026-08-04.md         the session before (overhead on days per branch)
    docs/ENGINEERING_RULES.md          binding (R1 coverage, R2 dual review, R3 IaC, R4 drift)
    docs/WARRANTY_TOOL_TIDAL_LAYER.md  the tidal layer, its gates, and why each exists

STATE: everything through PR #44 is merged and DEPLOYED and verified.
    API a32de84 (revision api-00245-lqt), frontend live, warranty plugin 1.6.1 on staging.
    Full suite green (exit 0); 3 skips, all environmental or a deliberate refusal to invent
    Tim's numbers. Jon's working tree has UNRELATED article/Wendy changes in it — leave them
    alone; deploy from a clean worktree (§6) because deploy.sh refuses a dirty tree.

DO THIS, IN ORDER — §5 of the continuation has the detail and the reasoning:

P1. MAKE THE VIDEO DESCRIPTION EDITABLE. Josh asked for "auto-generated, allowing us to review
    and EDIT it before posting" and I shipped it read-only. He is blocked on this today.
      - add PATCH /video/{video_id}/description (role approve_video) taking {description}
      - set description_model = "edited" on a manual save, so a human-written caption is
        distinguishable from a generated one — otherwise a future "regenerate everything the
        old model wrote" silently destroys hand edits
      - VideoApproval.tsx: read-only <div> -> <textarea> + Save, dirty-tracked so a reload
        cannot silently discard; keep Regenerate but warn before overwriting an edited caption
      - test the PATCH round-trip AND the model-flag flip

P2. LOAD JOSH'S MASTER PROMPT into VIDEO_DESCRIPTION_PROMPT (currently EMPTY, so the generic
    DEFAULT_PROMPT fallback is what runs). 10,962 bytes, 18 sections, from his 2026-08-11 13:41
    email "Social media prompt + Metal Roof Clip Spacing Sheet". IT IS ALREADY IN THE REPO at
    docs/josh-social-caption-master-prompt-2026-08-11.txt (no need to re-fetch mail). It contains NO {transcript} placeholder — render_prompt appends the
    transcript in that case by design, so it works as-is. Generate one description afterwards and
    READ IT: his prompt targets Instagram/Facebook captions, a different register from today's
    default, so confirm the output is what Perkins actually wants to post.

P3. REBUILD THE WARRANTY PAGE from Josh's clip-spacing reference data
    (source image saved at docs/perkins-metal-clip-spacing-2026-08-11.png) — use the DATA, not the
    marketing artwork (Jon: "use the images and text as you fit to best display its reference
    data, not a style guide"). Two-column layout Jon asked for: RESULTS column (the verdict the
    visitor came for) beside a DETAIL column (the facts that justify it), so the important
    numbers stay visible while scrolling. Keep the summary. Replace placeholder uplift values:
        Metal Alliance  1.5" mechanical seam  12" O.C. clips  -218.8 PSF
        Gulf Coast      Versaloc 1.5"          8" O.C. clips  -189.25 PSF
        Englert         Series 1300 1.5"       approved assy  -165 PSF
        clips on a 20' panel: 12" O.C. -> 20, 8" -> 30, 6" -> 40
        thesis: more clips != stronger roof; strength is the TESTED ASSEMBLY (PSF). CCC1331944.
    wp-plugin/perkins-metal-warranty/assets/guide.json already holds uplift[] with
    panel/attachment/psf/mph/hvhz/note — that is the shape to fill.

P4. VERTEX SDK MIGRATION — ITS OWN PR, do not ride it along with anything else. The stated
    removal date (2026-06-24) has ALREADY PASSED and adapters/llm.py still imports
    vertexai.generative_models / vertexai.language_models. google-genai 2.10.0 is already
    installed. New surface: genai.Client(vertexai=True, project, location) ->
    client.models.generate_content(...) / client.models.embed_content(...).
    TWO TRAPS, verify both before merging:
      - VertexLLM.chat reads response.usage_metadata for the per-tenant token meter; the new
        SDK's response shape differs and a silently-zero meter is worse than a crash
      - embeddings are embed_dim=3072; a wrong output_dimensionality mismatches the vector
        corpus SILENTLY (retrieval degrades, nothing fails)

P5. HOUSEKEEPING (deletion over addition). adapters/broll_providers.py has ZERO importers —
    delete it. adapters/media_cleanup.clean_audio has NO callers — wire it into render_job or
    remove it. Decide, don't leave it.

BLOCKED ON JON — ask, do not guess (§3):
    - the email to Tim + marco + josh with the NHD analysis and the C-8 open item is UNSENT.
      Tim's 2026-08-06 message DOES carry Screenshot 2026-08-06 at 7.42.43 AM.png (986 KB) and a
      1.4 MB JPEG — read those first; one is the address Jon meant. I twice said there was no
      attachment because I used Graph $filter instead of $search. That was wrong.
    - Josh must RETRY the brand-video upload. The IAM binding is verified present but was never
      executed (impersonating api-run-sa needs Token Creator; Policy Troubleshooter is disabled).
      If it fails now the logs finally say why — logger.exception was added to both GCS handlers.
    - C-8 canal, North Miami: two gauges 122 m apart, NE 135 ST reads 465 uS/cm (fresh) and
      UPSTREAM OF S-28 reads 20,450. The fresh one wins on channel distance so a house at the
      S-28 end reads 2,812 ft -> warranty-safe. Is the salt line in the right place?

THE PATTERN THAT KEEPS BITING (§0) — five defects this session, one shape: the write path
worked and NOTHING READ IT BACK. A config key with no reader, a description stored but never
returned, an endpoint whose service account could not write, a VPN fallback with no log line.
VERIFY BY EXERCISING THE RUNNING SYSTEM: call the endpoint, read the row back, check the PROD
job config, grep the READER not the key. And never verify a thing against its own upstream
source — the review that blessed the previous tidal fix checked it against OSM, which is where
the layer's data comes from, so it agreed with itself.

GOTCHAS:
    ⚠️ Graph mail: use $search="...", NEVER $filter=from/emailAddress/address eq '...' — the
      filter is case-sensitive and silently returns 0 for messages that exist.
    ⚠️ `npm run build` is the gate, NOT `tsc --noEmit`.
    ⚠️ tidal.geojson.gz is GITIGNORED and must be regenerated before zipping the plugin;
      checker.js prefers it because the WP host sends no content-encoding (22 MB vs 2.88 MB,
      measured 38-93 s vs 4-10 s against the live host).
    ⚠️ DB sessions: session.info["tenant_id"] = 1 is REQUIRED before the first query (RLS), or
      session.info["platform_scope"] = True for platform_config.
    ⚠️ ADC is stale — cloud-sql-proxy needs --gcloud-auth to borrow the CLI account.
    ⚠️ deploy.sh REFUSES a dirty tree (R3-ENFORCE). Jon's tree is dirty with unrelated work, so
      deploy from `git worktree add --detach /tmp/deploy-wt main` + `cp .env`, then remove it.
    ⚠️ scripts/apply_migrations_adc.py replays from 0013 and ignores DB_URL — apply single
      migrations directly instead (psycopg runs a multi-statement script in ONE execute; do not
      split on ';', the comments contain semicolons).

COMMITS:
    The hook requires `Refs #N <pct>%`, `Closes #N`, or `No-Task: <reason>` ALONE on its line —
    trailing prose breaks the regex and the commit is rejected.

VERIFY BEFORE CLAIMING DONE:
    .venv/bin/python -m pytest tests -q -p no:warnings        # full suite, ~15 min
    .venv/bin/python scripts/check_tidal_layer.py             # pins; non-zero on failure
    .venv/bin/python scripts/mutate_tidal_gates.py            # 9/9 gates must catch their mutation

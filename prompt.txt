Resume perkins-roofing/video-archival.

READ FIRST:
    CONTINUATION-2026-08-03.md         ← START HERE. §0 is the pattern; §3 is what to do next.
    CONTINUATION-2026-08-02-night.md   the backlog audit this session executed
    CONTINUATION-2026-08-02-eve.md     Tim's four answers, shipped
    docs/ENGINEERING_RULES.md          binding (R1 coverage, R2 dual review, R3 IaC, R4 drift)

main pushed, tree clean, CI green, ZERO open PRs.
DEPLOYED AND VERIFIED: API platform:b3b8144 (tag matches main), SPA /assets/index-CPfttX26.js.
Migrations through 0055. Live configs: jupiter v31, miami v32, naples v31.
GCP spend budget LIVE ($200/mo, 0.5/0.8/0.9/0.95) — there was none at all before 2026-08-02.
Closed 2026-08-02/03: #359 #360 #387 #388 #417 #452.

Confirm before trusting any of this:
  git log --oneline -1 && git status --short
  gh pr list                                   # expect none open
  gh run list --workflow=deploy.yml --limit 3  # MATCH THE SHA to main
  gcloud run services describe api --region us-central1 \
    --format='value(spec.template.spec.containers[0].image)'    # tag must EQUAL main
  B=$(curl -s https://app.perkinsroofing.net/ | grep -o '/assets/index-[A-Za-z0-9_-]*\.js' | head -1)
  curl -s "https://app.perkinsroofing.net$B" | grep -c 'Copy pricing config'   # expect 1
    # ^ THAT LAST CHECK IS NOT DECORATION. It is what caught prod running a merge behind while
    #   every CI run was green. Check the SERVED artefact, not the run status.
  .venv/bin/ruff check core adapters api jobs
  .venv/bin/python -m pytest tests/ -q          # ~15m. CI runs the WHOLE tree.
  bash scripts/drift_check.sh                   # needs TF_VAR_cloudflare_api_token; it fetches it

WHAT TO DO, in order. Everything here is UNBLOCKED — nothing waits on Tim, Jon, Wendy or Marie.

 1. #342 — BUILD THE EVAL HARNESS. `evals/` is confirmed absent. recall@k, precision@k,
    groundedness over the 832-video corpus, wired to CI. Supersedes #333 (app/eval.py, never
    wired, never run).
    WHY IT LEADS: it is the missing instrument for a known-broken thing. Memory
    `claim-checker-retrieval-broken` records the claim checker measured at 0/3 precision, and
    every grounding fix so far shipped unmeasured. Until this exists, no retrieval change can be
    shown to help or hurt.

 2. #429b — the 890 Knowify contracts. Analysis, not code.
    ⚠️ Knowify deliverables carry ContractId, NOT ProjectId. The wrong join once returned a clean,
    confident, WRONG answer. Money is CENTS on the MCP path and dollars on the mirror.
    #429a is already settled — NOT a defect, see CONTINUATION-2026-08-03.md §4. Do not redo it.

 3. #459 — 2-minute tracker fix. It asks to split #429; §4 already answers half. Close or re-scope.

 4. #402 (~5 min) and #382 — buildable now. wp-plugin/perkins-metal-warranty/ already exists.
    ⚠️ Content lands on STAGING until the WordPress cutover (#456, Jon's). Build them; do NOT
    report them as customer-facing.

DO NOT START (blocked, and on whom — detail in §5):
    #492  stucco metal 10x — TIM. The only open item with real money on it.
    #444  BigQuery billing export — a permission perkins-deploy-sa does not have (403 READING the
          billing account). ⚠️ Do NOT re-derive this from `gcloud billing accounts list` returning
          0 items — that is a DIFFERENT permission. Terraform only needs budgets.create/get, which
          it HAS, and the budget applied first try. Check terraform state, not gcloud.
    #504  register the CompanyCam webhook — operational, ~10 min, recipe in
          docs/2026-07-28-companycam-credentials.md.
    #456  WordPress cutover — Jon + Wendy. Gates the whole content wave.
    #472  SquareQuote rebuild — Jon deferred it 2026-08-02: "we're going to use roofr for now."

HOW TO WORK HERE — this is the part that actually matters:

    FIVE defects last session were one shape: A CORRECT THING NOTHING COULD REACH. Unread config,
    engine inputs the API could not express, a branch with no pricing config, a renderer with no
    writer, a terraform resource behind two missing APIs. In FOUR of the five, something ASSERTED
    it was done — a config note, a green suite, a tracker entry, a `count` guard.

    Not one was caught by reading code or by the test suite. Every one was caught by exercising
    the running system:
      * config rule      -> grep for the READER (accessor, call site), never the key
      * new API input    -> call the real endpoint and assert the line appears; a 200 proves nothing
      * new config key   -> confirm it exists in PROD's active config, not just the fixture
      * renderer field   -> establish WHICH renderer; _assemble_review_text feeds the AI review,
                            NOT the customer document
      * terraform        -> plan AFTER apply; "Apply complete!" is not convergence

    Write two tests that fail for DIFFERENT reasons — one that the surface can express the thing,
    one that it forwards it. A field that parses and is then discarded fails exactly as silently
    as one never declared.

    THE BACKLOG LIES IN BOTH DIRECTIONS. Verify before building: #388's "add a 4th branch" was
    already done, #417's audit said "nothing here is implemented" when two of thirteen were, and
    #417 G11 was described as unimplemented while it was billing live in all three prod configs.

DEPLOY RULES:
    CI is the ONLY path to prod: merge -> ci -> deploy (image AND SPA). Do NOT run
    scripts/deploy.sh by hand — it SKIPS the R4 drift gate, which is how GCP got ahead of git and
    the next plan wanted to DESTROY a just-created budget.
    R3: git -> apply, never the reverse. Commit and merge BEFORE `terraform apply`.
    ⚠️ Merging two PRs close together used to let the OLDER deploy finish last and overwrite the
    newer one, silently, with every check green. A supersede guard now blocks that — but it has
    NEVER BEEN GENUINELY EXERCISED. Next double-merge, check the superseded run for its ::notice
    instead of assuming.
    ⚠️ `npm run build` is the gate, NOT `tsc --noEmit` — --noEmit has passed on code that fails
    the build, twice.

COMMITS:
    The hook requires `Refs #N <pct>%`, `Closes #N`, or `No-Task: <reason>` ALONE on its line —
    trailing prose breaks the regex and the commit is rejected.

Resume the Perkins platform + Ez-Bids multi-tenant build. AUTOPILOT everything buildable.
Jon is doing the IG/TikTok social-API setup himself (docs/SOCIAL_API_SETUP.md); Tim is sending
real estimates/proposals. Read IN ORDER first:

1. CONTINUATION-2026-07-10-pm.md          — full handoff (state, resume tasks, gotchas)
2. Memory index (auto-loaded)             — esp. perkins-pricing-verified, ezbids-degenito-cloudflare
3. docs/ENGINEERING_RULES.md              — R1–R5 binding
4. mcp__jarvis-memory__sync then my_tasks — live blockers (Perkins v2 project)

PRIMARY TASK — analyze Tim's 2 pricing workbooks and resolve why there are "so many":
  - Sloped/regular: Drive id 1ptSxJYPumUKtxJk66JgbZ8tljVJMbyCAyaSyz_BCwg0
  - Low-slope/flat: Drive id 1SGLYoOIU13nILqGxJCIxuVZ8A2I_BTEjJ03PUuuRteo
  The Drive connector reads TAB 1 ONLY. Each workbook likely has per-branch tabs
  (miami/jupiter/naples). Enumerate ALL tabs (Sheets API spreadsheets.get for titles, then
  per-tab reads — or ask Jon to point at tabs), map the full roof-type × branch × zone
  structure, reconcile vs infra/fixtures/pricing_config_exhibit_b.json. KEY FACT: Miami sloped
  tab 1 == Exhibit B HVHZ exactly; HVHZ=Miami-Dade+Broward only; zone(HVHZ/FBC) and
  branch(miami/jupiter/naples) are SEPARATE axes; jupiter/naples configs currently duplicate
  Miami and need real values. See memory perkins-pricing-verified.

THEN AUTOPILOT (sonnet impl subagents; opus for plan/review/security only):
  1. Estimator v2 (task #10) — day-based overhead (per-series days to the half-day) + absolute-
     dollar profit mode. Spec: docs/superpowers/specs/2026-07-10-estimator-v2-tim-feedback.md.
     Tim's 2 worked examples = golden cases. OH-mode + profit-mode selectable so Exhibit B
     goldens still pass.
  2. Ez-Bids W0 (plan APPROVED) — DB-backed CORS, env cleanup, brand tokens. Then W1 (explicit
     tenant-1 binding, Perkins-smoke gate), W2 (ezbids.degenito.ai domain onboarding — degenito
     CF token READY, zone 63d609f219b48dafafdb02afa31d403f). Plan:
     .omc/plans/ralplan-ezbids-multitenant-DRAFT.md. Sync PRD/TRD/DDD to the council-revised plan first.
  3. (optional) Clip Studio v2; Squares imagery-fields fix; DMARC p=reject after ~2wk clean reports.

NON-NEGOTIABLE DISCIPLINE:
- TDD fail-first. Gate = the CI-EQUIVALENT gate, NOT a Docker-having local gate:
  ruff check core adapters api jobs + `pytest tests/ -m "not postgres" --cov=core
  --cov-config=.coveragerc --cov-fail-under=100` (background to /tmp log + EXIT marker) +
  `cd web && npm run build` for SPA changes. Backend CI has no testcontainers — postgres tests
  SKIP there, so coverage they alone provide fails CI while passing locally.
- Subagents = FILE EDITS ONLY, never git; give each a NON-overlapping file boundary; main lane
  commits (ideally gate on a clean clone of the commit). Implementation subagents = sonnet.
- Commit + push at each green milestone. strict=True is LIVE (stamp every session). Migrations
  additive .sql. R3 infra via terraform only. Output-compressor garbles piped JSON → write to
  file, parse separately. Deploy via run_in_background (not `&` — gets SIGHUP'd).
- Do NOT relitigate locked/approved decisions (Ez-Bids plan is APPROVED; council findings folded).
  Surface hard-to-reverse prod actions.

STATE: everything from the 2026-07-10 sessions is LIVE (strict tenancy, Contract-FAQ, domain +
email auth, YouTube posting, Squares/Solar, estimator+low-slope, Proposals/Quotes UI, Clip Studio
v1, ask-cache, email compose, TinyMCE fix, YouTube-link fix + 675 rows recovered). CI GREEN.
Ez-Bids planned/council-hardened/APPROVED. Waiting on Jon (IG/TT apps) + Tim (golden files,
Jupiter pricing, pm_incentive sign-off).

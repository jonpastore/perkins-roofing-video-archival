Resume perkins-roofing/video-archival.

READ FIRST:
    CONTINUATION-2026-08-01.md        the bid-project work (this) — READ §3 BEFORE ANY DEPLOY
    CONTINUATION-2026-07-31-pm.md     warranty tool statewide + the OAuth account-scoping fix
    CONTINUATION-2026-07-31.md        the measured-salinity build those follow

HEAD bc771b3, pushed, tree clean, full suite PYTEST_EXIT=0, terraform plan exit 0.
Warranty plugin 1.3.4 live on staging. Prod migrations through 0051.
⚠️ 0052 IS COMMITTED AND NOT APPLIED — deliberately. See "THE ONE TRAP" below.

Confirm before trusting any of this:
  git log --oneline -1 && git status --short
  git log --oneline origin/main..HEAD          # expect empty
  bash scripts/plan_as_ci.sh                   # expect "plan exit code: 0"
  gh run list --limit 4                        # MATCH THE SHA
  gcloud run jobs executions list --job salinity-sweep --region us-central1 --limit 3
    # expect a RECENT one by scheduler-sa with succeededCount=1

⛔ STAY ON STAGING. PlatformConfig.WP_URL = https://1228404.us6.myftpupload.com is CORRECT.
⛔ DO NOT CHANGE PRICING without Jon. Live: jupiter+naples overhead_basis="series"; miami "branch"
   at $4,250/day; concurrent_crews deployed but UNSET everywhere.

════════════════════════════════════════════════════════════════════════════════
⚠️ THE ONE TRAP: MIGRATION 0052 IS COMMITTED, NOT APPLIED, AND THE ORM IS NOT IN GIT
════════════════════════════════════════════════════════════════════════════════
MIGRATIONS HERE ARE APPLIED BY HAND. Neither scripts/deploy.sh nor CI runs them — verified.
So the ORM half was written and then REVERTED ON PURPOSE. If you re-add BidProject /
Estimate.bid_project_id / Proposal.bid_project_id BEFORE applying 0052, every SELECT on estimates
and proposals fails in prod. Schema first, ORM second, always.

  DB_URL=postgresql+psycopg://app:$(gcloud secrets versions access latest --secret=db-password)@127.0.0.1:5432/perkins \
    .venv/bin/python scripts/apply_migrations_adc.py

0052 is additive and idempotent (new table, 3 nullable columns, IF NOT EXISTS). Applying it alone
changes no behaviour.

▶ START HERE — ONE OF THESE, ASK JON WHICH

  1. SLICE 2 of #430 — persistence. Apply 0052, re-add the ORM, add POST /estimator/project-quote
     returning the roll-up and persisting N estimates + one bid_project. Snapshot gains buildings /
     project_items / project_totals; keep roof_type as the dominant building and num_squares summed
     so core/proposal.py:_REQUIRED_SNAPSHOT_KEYS needs no change.

  2. SLICE 3 — proposal surface. ⚠️ READ THIS BEFORE STARTING: web/src/pages/Proposals.tsx:414-419
     re-quotes exactly ONE estimate and overwrites quote_snapshot. On a project snapshot that
     SILENTLY DESTROYS EIGHT BUILDINGS. Hard-gate it in the same slice that first creates a project
     proposal. Also api/routes/proposals.py:143-167 _assemble_review_text reads ONE roof, so the
     LLM send-gate would review one building and green-light a nine-building contract — a silently
     weakened safety gate. And api/routes/estimator.py:441-450 fires min_margin_breached per
     building under project scope.

  3. SEND TIM THE SIX QUESTIONS (below). None are defaultable and one is worth $8-16k.

  4. IAM: api-run-sa cannot create a secret (has secretAccessor / secretVersionAdder / viewer,
     nothing granting secrets.create), so the OAuth connect flow 502s. Needs a Terraform grant —
     a security-scope decision, deliberately left for Jon.

DO NOT RE-DERIVE (all measured, all committed)
  - Slice 1 of #430 SHIPPED and scores against Tim's real bid: project total ours $390,230 vs his
    $381,288 (+2.3%), profit $30,790 vs $30,363 (+1.4%). Bus Stop went +84.9% -> -24.3%.
    Re-run: DB_URL=... .venv/bin/python scripts/validate_against_evergrene.py
  - #430's OWN DIAGNOSIS WAS WRONG and it changed the plan. It reads as two pricing errors
    cancelling. It is ONE systematic over-charge (+10.2% to +84.9% on EVERY building) masked by
    $116,420 of project scope we never quoted. Fixing only the fees/floor would have swung the bid
    from -9% to about -23%. Both halves had to land together.
  - #449's SPEC DOES NOT RECONCILE. "$2,500 per site per WEEK" is 17 x $2,500 = $42,500 on
    Evergrene against Tim's actual $30,363 — 40% over. Default basis is 'project'; 'week' exists
    and BOTH numbers are returned on every call. Do not silently switch to 'week'.
  - FOUR fees are site-scoped: delivery_plywood_vents, new_bonus_values, permit_processing (a
    COUNT, not a scope) and tile_dumpster. ⚠️ tile_dumpster_count is a ceil(), so per-building
    calls round up per building — 14 loads billed for a 10-load site. Recomputed over SUMMED
    squares. stories_3_5_delivery_chute stays per building.
  - Measured split: fixed fees ~$24,000 of the error, floor ~$5,000. #449 leads with the smaller.
  - Tim's model, decoded from his FORMULAS (tests/fixtures/golden/evergrene_project.json):
    price/sq = (base + OH/sq) x markup, markup 1.10-1.14 PER BUILDING, base 750-1085 PER BUILDING.
    OH = (demo + metals_demo + delivery) x $885 + tile_install x $745.
    His General Conditions ((22,800+9,000) x 1.15 = $36,570) is referenced by NO total formula.
    His stated total K42 EXCLUDES the Clubhouse Flats ($38,480) AND GC — compare like for like.
    He hand-edits the OH formula per row: that is DRIFT, not a model. Do not reproduce it.
  - His day rates vs ours: tile install $745 MATCHES exactly; demo $885 vs our $1,050; metal
    install $735 vs our $850 (ours 15% high). His $1,050 appears only as metal-scope mobilisation.

SIX QUESTIONS FOR TIM (he has never stated any of them — that is the test for asking)
  1. One permit per structure or per site on a multi-building job? ($500-$9,000 swing)
  2. Is new_bonus_values ($1,350) per mobilisation or per roof completed?
  3. IS COMMISSION PAID ON GENERAL CONDITIONS? ~$8-16k on Evergrene alone.
  4. Is the General Conditions markup always 1.15, or per bid?
  5. What are the bare +4250 (K33) and +2550 (K35) on the Evergrene sheet? Unlabelled anywhere.
  6. Must a proposal list each building's own address? Two Evergrene gates are on different roads.
  PLUS: on Evergrene you billed demo at $885 and metal install at $735; our config carries $1,050
  and $850. Are commercial rates different, or have the numbers moved since the 7/24 email?

STILL OPEN
  - Miami charges its whole office day per job -> ~$2,087/sq HVHZ tile vs $1,113/sq accepted.
  - #447(3) commission_pct keyed by slope type, not per salesperson as PRICING_RULES §11 says.
  - #444 GCP budget: Billing API disabled, SA cannot enable it. TF resource waits on
    var.billing_account.
  - REACH_MI residual: 8 of 18 far live gauges never snap to mapped water (OSM maps those wide
    rivers as natural=water POLYGONS, not waterway lines).
  - Salinity banking is correct and INERT: every latest-only gauge is also stale (the two sets are
    byte-identical), so nothing banks until a dormant station resumes.
  - #445 code done; the Wendy/Crypt Keepers review meeting is separate. naples carries Jupiter's
    $1,400 + office_men=None.

GOTCHAS
- ⚠️ MIGRATIONS ARE APPLIED BY HAND. Schema first, ORM second. See THE ONE TRAP.
- ⚠️ tile_dumpster_count is a ceil() — anything calling it per building over-counts.
- ⚠️ profit_guidance is ABSENT from the estimate result unless it is a daily-overhead quote.
- ⚠️ pricing_configs columns are `config` and `is_active`, NOT config_json/active. Roof types:
  13_tile | barrel_tile | 3tab_shingle | dimensional_shingle | standing_seam_metal.
- ⚠️ Do NOT `source .env` before GCS work: it sets GOOGLE_APPLICATION_CREDENTIALS to
  ./infra/vertex-dev-sa.json, A FILE THAT DOES NOT EXIST. Use
  $HOME/.config/gcloud/perkins-deploy-sa.json.
- ⚠️ DB_URL in .env is sqlite. Platform DB needs postgresql+psycopg://... over the Cloud SQL proxy.
  `+psycopg` — psycopg2 is NOT installed and plain postgresql:// selects it and fails.
- ⚠️ .dockerignore ALLOWLISTS scripts/. A job importing a NEW script needs !scripts/<name>.py or
  the image ships an EMPTY namespace package ("unknown location"). Guarded by a test now.
- ⚠️ The job container has NO writable $HOME. Path.home() raises PermissionError '/home/appuser'.
- ⚠️ GREEN CI + CLEAN TERRAFORM + A FIRING SCHEDULER PROVED NOTHING, TWICE. Both salinity-sweep
  bugs existed only inside the built image. Verify a job by RUNNING it and reading its stdout.
- ⚠️ Gauge readings are 30-day medians over a SLIDING window — they move. The ORDERING (fresh
  landward of a structure, salt seaward) is the durable fact, not the number.
- ⚠️ OSM `tidal=yes` means water LEVEL, not salinity. The St Johns is tidal ~160 mi inland and
  fresh. Exempting `tagged` from the clip shipped a false VOID on freshwater Dunns Creek.
- ⚠️ qwen3.6 passed all three review areas as "Correct" and was wrong twice. Local review is a
  second opinion, NEVER a gate. Security-critical review stays on Claude.
- ⚠️ resolved_wp_url() swallows every exception and returns "". Empty = config/driver error.
- ⚠️ A bare `until` spin with no sleep burns the whole timeout; `sleep N` chained after a command
  is blocked by the harness.
- ⚠️ Printing .env leaks WP_PWD (it reached a transcript 7/31) — worth rotating.
- Pre-push: .venv/bin/python -m pytest tests/api tests/core tests/adapters tests/jobs tests/tenancy
  (~8m). The EXIT CODE is the evidence — the summary may not flush to a redirected log.
- CI gates `ruff check core adapters api jobs` FIRST, then pytest --cov-fail-under=97. tests/ is
  NOT ruff-gated; tests/jobs/test_search_indexing_job.py carries 9 pre-existing errors.
- WE HAVE A SERVICE ACCOUNT: ~/.config/gcloud/perkins-deploy-sa.json reads the tfstate bucket, so
  terraform apply works without the reauth `invalid_rapt` demands:
    cd infra && unset GOOGLE_IMPERSONATE_SERVICE_ACCOUNT
    export GOOGLE_APPLICATION_CREDENTIALS=$HOME/.config/gcloud/perkins-deploy-sa.json
    export USER_PROJECT_OVERRIDE=true GOOGLE_CLOUD_QUOTA_PROJECT=video-archival-and-content-gen
    export TF_VAR_cloudflare_api_token="$(gcloud secrets versions access latest --secret=cloudflare-api-token 2>/dev/null)"
- Cloud SQL proxy: setsid nohup ~/bin/cloud-sql-proxy \
    video-archival-and-content-gen:us-central1:video-archival-and-content-gen-pg --port 5432 &
  Queries need `set app.tenant_id='1'`.
- Rebuild + ship the warranty tool:
  .venv/bin/python scripts/fetch_salinity_readings.py     # or let the hourly sweep do it
  GOOGLE_CLOUD_PROJECT=video-archival-and-content-gen .venv/bin/python scripts/build_tidal_layer.py
  .venv/bin/python scripts/check_tidal_layer.py           # asserts; non-zero on regression
  .venv/bin/python scripts/validate_tidal_against_gauges.py
  bump PERKINS_MWC_VERSION, zip wp-plugin/perkins-metal-warranty, then
  set -a; source .env; set +a; export WP_LOGIN_PW="$WP_PWD"
  PYTHONPATH=. DB_URL=<platform> .venv/bin/python scripts/wp_install_plugin.py <zip>  (WEB login)
- Tim's sheets via perkins-deploy SA + DWD impersonating tim@perkinsroofing.net. Cell COMMENTS
  carry values the visible cells do not, and comments are a DRIVE resource.
- EMAIL_SEND_MODE=test in prod. R7-R10 in docs/ENGINEERING_RULES.md are binding.

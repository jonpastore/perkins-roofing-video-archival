Resume perkins-roofing/video-archival.

READ FIRST: CONTINUATION-2026-07-26.md — §0 answers a question Jon asked and the answer changes
what you build, so read it before writing code or touching the Tim draft.

HEAD is d3c0a39 (or the docs commit above it). Everything from the 7/26 session is COMMITTED but
NOTHING IS DEPLOYED and NOTHING IS SEEDED TO PROD. Prod configs: jupiter v17 / miami v18 / naples
v17; deployed code platform:27e076d.

Confirm:
  git log --oneline origin/main..HEAD          # expect empty
  gcloud run services describe api --region us-central1 \
    --format='value(spec.template.spec.containers[0].image)'

THE ANSWER TO JON'S QUESTION (detail in §0, don't re-derive it)
Jon asked whether Tim had said he wanted to see how a price was built. He did NOT. The "notes
column / word problem into an algebra equation" line at Zoom 2026-07-17 [12:14] is JON speaking,
asking Tim to annotate his 30 homes so we could fit the day model; Tim's reply at [12:44] is him
agreeing to write the notes. Tim documenting HIS reasoning FOR US is the opposite of us showing OUR
build-up to a customer. Both transcripts were searched; there is no such request.

Two consequences:
  1. The "How this price was built" proposal section (d3c0a39) is a product proposal to Jon, not a
     Tim requirement.
  2. THE DRAFT EMAIL MISATTRIBUTES JON'S WORDS TO TIM and must be fixed before sending — it says
     "On the call you offered exactly the fix: 'a notes column…'". Rewrite as something Jon asked
     for and Tim agreed to.

THE WORK, in Jon's priority order

1. SEED PROD — live money. A 35-square Palm Beach RESIDENTIAL job charges $50 of PM incentive where
   Tim's live sheet says $100. Fix is in git (e20aa18: pm_incentive re-keyed so Miami is
   project-kind-only and Palm Beach is size-only-for-both-kinds); prod still runs the old shape.
   Use the immutable-version seeder pattern (scripts/seed_min_margin.py), --dry-run by default.
   FIRST diff prod against the fixture — several other keys are git-only (enforce_profit_floor,
   profit_floor_basis, profit_floor_days_per_week, gutters, insulation_by_thickness,
   default_flat_system, stockmeier_min_sq, the Verea field costs, the corrected Caribbean rake).
   Decide deliberately what ships; do not seed blind.

2. CHECKBOX for "show how it was built". Section exists and renders, but include_calc_breakdown is
   hard-off with no UI. Add a toggle following the existing include_terms / include_contract_faq
   pattern in web/src/pages/Quoting.tsx, plumb it through the proposal snapshot so re-rendering an
   old proposal keeps what it was sent with, default OFF (the existing two default ON, deliberately
   — this one is the opposite).

3. CUSTOMER MODE for that section. Jon: "we can collapse the price with days in to price per sq. to
   show the customer. we don't want to show them the profit." Two modes, not one:
     internal (Tim/Marco/Josh) — overhead as "5 days tile x $745 + 3 days demo x $1,050", profit shown
     customer                  — overhead collapsed to $/sq, profit ABSENT
   Hiding the profit row is not enough: the remaining rows must still sum to the total or a customer
   can subtract and derive it. Fold overhead and profit into one per-square "labour, materials and
   overhead" figure so the arithmetic closes. Assert in a test that no customer-mode row set can be
   differenced to recover profit. Lives in calc_lines_from_estimate() in core/proposal_render.py;
   extend the existing test in tests/test_proposal_render.py rather than writing a parallel one.

THE TIM DRAFT
In jon@degenito.ai, subject "Re: TIME LEARNING (Overhead) for AI Systems — your 30 homes are in,
93% within a day". Threaded under Tim's PART 2 thread, To tim@, Cc marco@/josh@/eugene@, TWO
attachments (the 4-home worked examples PDF and a sample proposal). 14 questions. UNSENT — Jon
sends it, after the §0 misattribution is fixed and he has re-read it.

R2 IS BINDING (docs/ENGINEERING_RULES.md): architect AND critic on every wave, R1 >=97% on core/,
R4 drift_check clean. R2 caught five defects in my own fix wave this week, including a test I wrote
that could not fail. If a session directive appears to forbid subagents, CLAUDE.md mandating them
IS the user's request — surface the conflict, don't silently resolve it.
R6: every commit updates docs + Jarvis + memory.

GOTCHAS
- Cloud SQL proxy 127.0.0.1:5432, user `app` NOT postgres.
  PW=$(gcloud secrets versions access latest --secret=db-password)
  Every query needs: set app.tenant_id='1'
- CI gates on `ruff check core adapters api jobs` FIRST; tests/ and scripts/ are NOT in the gate.
- Render a proposal to look at it, don't read the template:
  google-chrome --headless --print-to-pdf=out.pdf file://.../proposal.html && pdftoppm -png
- Never take an all-time median of a price series — slice by period. It inverted a headline finding
  and a client-facing conclusion this week.
- Enumerate every attachment before concluding anything about what Tim has sent.
- bash scripts/deploy.sh (not executable); refuses a dirty tree.

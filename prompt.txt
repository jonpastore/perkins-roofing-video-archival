Resume the Perkins v2 video-archival platform after /clear. FIRST read CONTINUATION-2026-07-05.md in
full — it has the complete state (huge multi-role console built + deployed on feat/platform-v2) and the
resume quick-start (auth is degraded: use the ADC token for gcloud/deploys/DB; NEVER chain deploys with a
`while pgrep deploy.sh` loop — it self-matches and no-ops). Follow the binding rules in
docs/ENGINEERING_RULES.md (R1 core coverage, R2 architect+critic review, R3 100% IaC git→apply — Terraform
for GCP, Ansible for cerberus, scripts/apply_migrations.sh for schema, scripts/deploy.sh for the image;
NO ad-hoc gcloud/create_all changes).

IMPORTANT — switch all generation to the CLOUD (Vertex): use
  LLM_BACKEND=vertex LLM_MODEL=gemini-2.5-flash EMBED_BACKEND=vertex EMBED_MODEL=gemini-embedding-001
for every job/run. The local cerberus Qwen path had integration issues (empty structured-JSON on articles;
embed recursion — now fixed) and Jon asked to move off it. Do NOT set LLM_BACKEND=ollama.

Then finish the remaining tasks, in order (details + snippets in the continuation):

1. Finish the FAQ answers on Vertex. 5,189 questions are mined; a Vertex answer run
   (`python -m jobs.prime_backlog --answers 6000`, LLM_BACKEND=vertex) is in flight — verify all
   faq_entries are status='answered' and re-run to completion (resumable, ~$2 total). Confirm counts.

2. Regenerate the 20 bad Video-Approval proposals (they used the fixed 0/.25/.5/.75 fraction bug). Either
   POST /video/{id}/repropose for each, or delete the unapproved MiniSeries and re-run
   jobs.propose_series_job (now content-driven). Verify parts have real second offsets + good titles.

3. Prime article-cluster drafts for the top aggregated topics ON VERTEX
   (`python -m jobs.prime_backlog --articles N`, LLM_BACKEND=vertex). Verify real (non-stub) HTML content.

4. Publish the FAQ page to WordPress (POST /faq/publish-wordpress) once answers exist (needs WP creds).

5. Do a Playwright visual walk as jon@perkinsroofing.net (mint a custom token via infra/vertex-dev-sa.json →
   Identity Toolkit exchange → seed IndexedDB) across every console tab to confirm the big UI batch renders.

Deploy as you go (SPA: firebase deploy --only hosting; API: CLOUDSDK_AUTH_ACCESS_TOKEN=$(gcloud auth
application-default print-access-token) bash scripts/deploy.sh). Commit as you go; don't push unless asked.
External blockers (not yours to unblock): Meta/TikTok social creds (Mon), Workspace admin consent for
org-directory user invites, Resend domain verify. To give the GPU back to Whisper:
ansible-playbook local_llm.yml -e reclaim_gpu_from_whisper=false && ansible-playbook whisper.yml.

Resume perkins-roofing/video-archival.

READ FIRST:
    CONTINUATION-2026-08-11-eve.md     ← START HERE. §0 is state, §1 is the pattern, §4 is the plan.
    CONTINUATION-2026-08-11-pm.md      the session before (5 PRs shipped + deployed)
    docs/ENGINEERING_RULES.md          binding (R1 coverage, R2 dual review, R3 IaC, R4 drift)

STATE:
    THREE COMMITS ARE UNPUSHED AND NOT DEPLOYED — 13366a8, 4988fa7, ae949d3. They are green
    (pytest exit 0 captured directly, 0 FAILED, 5 skips with stated reasons). Prod runs the
    earlier state (revision api-00254-nvk). Nothing below is live until you push + deploy.

    ⚠️ JON'S WORKING TREE HAS SIX UNCOMMITTED FILES — core/article_criteria.py,
    core/article_repair.py, jobs/article_job.py, scripts/backfill_wendy_compliance.py,
    tests/core/test_article_{criteria,repair}.py. That is his in-progress Wendy "Related links
    x3" fix. LEAVE THEM ALONE. Last session I swept four of them into a commit with
    `git add -u core/` and had to reset and recommit to extract them. NEVER stage by directory
    in this repo — always name files explicitly.

DO THIS, IN ORDER — §4 of the continuation has the detail:

P1. ASK JON WHETHER TO PUSH + DEPLOY the three commits. They fix, among others, a HIGH that lets
    one upload stall the WHOLE API (a 44 KB PDF blocked the event loop for 40.5 s), a CRITICAL
    that printed Perkins' internal "$45.00 per SQ" upcharge on the PUBLIC warranty page, and a
    HIGH where the debug trace persisted into quote_snapshot that `sales` can read. Deploy
    commands are in §6. deploy.sh REFUSES a dirty tree, so use a clean worktree of origin/main.

P2. THE Q1 EMAIL IS DRAFTED AND UNSENT in Jon's Outlook (created via the gmail-enhanced MCP with
    account=jon@degenito.ai — the o365 MCP's refresh token is EXPIRED, 90-day inactivity). It
    asks Tim/Marco/Josh for the FL approval / NOA numbers and test conditions behind the Gulf
    Coast and Englert uplift rows, or agreement to publish Perkins-only. Ask Jon whether to send.

P3. PHASE 1 OF THE PROJECT-PAGES PIPELINE — wire the tag reads. Independent of every open
    question and unblocks the Building 77 demo.
      - adapters/companycam.py:91 reads `raw.get("tags")`, A KEY ABSENT FROM EVERY PAYLOAD, and
        core/companycam/mirror.py:61 writes the resulting []. That mirror column has ALWAYS been
        empty and nothing reads it. Fetch with ?tag_ids[]= instead.
      - Restrict api/routes/portfolio.py::_available_media to Projects-tagged photos and
        ProjectsVideo-tagged videos, sorted captured_at DESC (newest first — Wendy wants the
        gallery to read final -> start).
      - Put the tag ids in config, not literals.
      - Acceptance: Building 77 returns exactly 9 photos + 2 videos, not 312 and 10. Cross-check
        every returned id against /v2/photos/{id}/tags.

P4. Q3 PUBLISHER REWORK (~1 day) — Jon decided KEEP the nine /portfolio/ pages, so
    adapters/wordpress.publish_portfolio_post must post PAGES under parent 2358 instead of the
    avada_portfolio CPT it writes today. HOLD until Wendy replies to the Q1 email, in case her
    answer moves the target again.

P5. Q4 MULTI-VIDEO SCHEMA — Jon decided up to 4 videos per project. The project record has a
    single `youtube_url` field; it needs a proper relation, and VideoObject follows.

⚠️ BEFORE ANY PROJECT PAGE PUBLISHES: CompanyCam BURNS GPS INTO THE PIXELS. Jon's Q6 answer is
    "we have permission for all properties, but mask the property — neighborhood or town/city,
    never the exact address." The TEXT side is already right (city-slug links,
    portfolio_facts._address_number_risk refuses street numbers, person-name titles blocked).
    The PIXEL side is the gap — confirm the sanitiser covers the burned-in stamp.

~70% OF THE PROJECT PIPELINE ALREADY EXISTS. Do not rebuild it. core/portfolio_{content,criteria,
critique,facts,media,projects}.py, core/companycam/, api/routes/portfolio.py,
adapters/wordpress.publish_portfolio_post, scripts/portfolio_*.py. Per-image alt uniqueness,
service+location linking, FAQPage/VideoObject scoping, the publish gate mirroring
article_criteria, permission blockers and GPS sanitisation are all already built.

THE PATTERN THAT KEPT BITING (§1) — EVIDENCE THAT CANNOT ANSWER THE QUESTION. Three times last
session I checked something with a method that could not have detected the failure, and reported
the conclusion confidently:
    - "CompanyCam tag filtering is broken" — I ran ?tag_ids[]=...&per_page=5 and saw 5 rows with
      tags:[]. per_page=5 returns 5 either way and tags are not inlined. I never compared against
      the unfiltered count. The filter WORKS: 100 -> 9.
    - "suite green, exit 0" — I read the exit code of `pytest | tail`, which is TAIL's.
    - "Jon's files are untouched" — I never checked; git add -u had taken them.
CHECK THE THING THAT WOULD CHANGE IF YOU WERE WRONG. Compare filtered against unfiltered. Capture
$? before the pipe. git log --name-only after committing.

GOTCHAS:
    ⚠️ CompanyCam: ONLY the plural bracketed `?tag_ids[]=` filters. `?tag_id=`, `?tags[]=`,
      `?tag=` are SILENTLY IGNORED and return unfiltered results. Projects tag id 26926152,
      ProjectsVideo 26926154, Building 77 project id 79260538.
    ⚠️ Graph mail: use $search="...", NEVER $filter=from/emailAddress/address eq '...'.
    ⚠️ `npm run build` is the gate, NOT `tsc --noEmit`.
    ⚠️ tidal.geojson.gz is GITIGNORED and must be regenerated before zipping the plugin.
    ⚠️ DB sessions: session.info["tenant_id"] = 1 REQUIRED before the first query (RLS), or
      session.info["platform_scope"] = True for platform_config.
    ⚠️ ADC is stale — cloud-sql-proxy needs --gcloud-auth.
    ⚠️ deploy.sh REFUSES a dirty tree (R3-ENFORCE) and Jon's tree is dirty — deploy from
      `git worktree add --detach /tmp/deploy-wt origin/main` + `cp .env`, then remove it.
    ⚠️ scripts/apply_migrations_adc.py replays from 0013 and ignores DB_URL.

COMMITS:
    The hook requires `Refs #N <pct>%`, `Closes #N`, or `No-Task: <reason>` ALONE on its line —
    trailing prose breaks the regex and the commit is rejected.

VERIFY BEFORE CLAIMING DONE:
    .venv/bin/python -m pytest tests -q -p no:warnings > /tmp/suite.log 2>&1; echo "EXIT=$?"
    grep -cE "^FAILED|^ERROR" /tmp/suite.log        # expect 0 — do NOT read a piped exit code
    .venv/bin/python scripts/check_tidal_layer.py   # pins; non-zero on failure
    .venv/bin/python scripts/mutate_tidal_gates.py  # 9/9 gates must catch their mutation

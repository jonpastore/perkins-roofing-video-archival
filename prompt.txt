Resume perkins-roofing/video-archival after a /clear.

READ FIRST: CONTINUATION-2026-07-17-eve.md (full state of the Monday-demo wave) and
docs/plans/2026-07-17-zoom-analysis.md (Tim's estimating model + remaining action items).
docs/ENGINEERING_RULES.md (R1-R5) is binding. HEAD is on branch main, clean.

CREDENTIALS (unchanged): all Google automation runs under a NON-EXPIRING service account:
`export GOOGLE_APPLICATION_CREDENTIALS=/home/jon/.config/gcloud/perkins-deploy-sa.json`
(also in ~/.bashrc). NO interactive login ever needed; re-export the SA key if a call 401s.
Sourcing .env clobbers it → re-export after any `source .env`.

WHAT JUST SHIPPED (2026-07-17 evening, autopilot wave — ALL DEPLOYED + PROD-VERIFIED):
Branch management, gutters (Tim's style price list), existing_roof demo, time-based
overhead, full package menu, profit slider, YouTube connect. API image f9c13e8 on Cloud
Run; SPA on Firebase; prod configs seeded (all branches v3 = base+gutters+daily overhead).
Migrations 0041 (branches) + 0042 (pricing-config partial index) applied to prod.
The 7 features + click paths are in docs/plans/2026-07-17-monday-demo-walkthrough.md.

PRIMARY GOAL (Jon's ask, in priority order):
1. **Finish the Zoom action items** in docs/plans/2026-07-17-zoom-analysis.md that are NOT
   yet built (branch/estimating wave IS done). Highest-value next:
   a. **Roof-cuts custom calculator** — the ~$1,150 / ~2% gap between our PROTECTOR ($53,100)
      and Tim's Greener ($51,950) is the manual cut-adjustment (round-to-10ft material pieces,
      waste factors per cut type) Tim does by hand. Needs RoofR cut LFs (eaves/hips/ridges/
      valleys/rakes/wall-flashings) as estimator inputs + the cut math. See zoom-analysis §1.
   b. **RoofR ingestion** — no public API; Jon to get account access / call them; also try
      the website-widget data path + parse report PDFs into measurements+cut LFs.
   c. **Duration-training dataset** — Tim will label 20-30 RoofR reports (per-phase days) using
      docs/templates/duration-training-template.csv; then train a duration predictor.
   d. **Low-slope builder inputs** (deck type, coating system) — configs exist, builder UI doesn't.
   e. **CompanyCam** photo pull (Tim adding Jon to account; REST v2 + webhooks researched).
2. **Then the franchise/accounting track** (backlog B8-B10): Qvinci/QuickBooks-per-branch
   (Tim has 4 companies / 4 Knowify / 4 QB subs → branch level), franchisee royalty +
   marketing-fee ACH via Stripe, owner cross-tenant admin view.

BLOCKED ON TIM (chase these):
- Share the ORIGINAL calculator sheets (the shared copies have no cell comments — comments
  hold the material↔price mapping needed to auto-populate config from the price book).
- Provide GC branch pricing values (GC quotes 503 until then — expected).
- Provide per-branch daily overhead if different from the seeded baseline (Miami OH > Jupiter;
  Naples zero right now — seeded values are Tim's general Zoom numbers, same for all branches).
- Re-mint / connect the YouTube channel-owner token (click Connect on Comments page, pick the
  Perkins-channel-owning account) to clear the reply-403.
- Gutters: proposal vs email had 2 tiny discrepancies (7" 2-story $18.30 vs $18.20; 6" 2-story
  $12.55 vs $12.95) — confirm which is right; seeded values use the email.

OPERATE:
- Prod DB: `/tmp/cloud-sql-proxy --port 5432 video-archival-and-content-gen:us-central1:video-archival-and-content-gen-pg &`
  then DB_URL=postgresql+psycopg://app:$(gcloud secrets versions access latest --secret=db-password)@127.0.0.1:5432/perkins
- Deploy API: `bash scripts/deploy.sh` (needs CLEAN tree). SPA: `cd web && npm run build &&
  npx --no-install firebase deploy --only hosting:app --project video-archival-and-content-gen`.
  (Firebase multi-site: targets `app` and `setback`; see web/firebase.json + web/.firebaserc.)
- Migrations: apply directly (0041,0042 already applied). Pricing configs are versioned +
  immutable; ONE active per branch (partial index, migration 0042). Seed scripts create a new
  active version: scripts/seed_gutters_config.py, scripts/seed_daily_overhead_config.py.
- Config edits from Admin UI create a new inactive version then activate (works post-0042).
- Prod e2e recipe: mint a smoke admin token via firebase_admin custom-token → ID token →
  hit the API; DELETE the smoke user after (see this session's prod_e2e approach).

ENVIRONMENT GOTCHAS (still true):
- SQLite (test DB) does NOT enforce VARCHAR limits, RLS, OR partial-index predicates — prod
  Postgres does. This class has bitten twice (property-500, the 0042 config-version cap). When
  a constraint/limit matters, test on the PG harness (scripts/test_pg.sh) too.
- `ps -eo pid,cmd | grep "[c]loud-sql-proxy"` — never pkill -f your own shell's pattern.
- articles/videos are RLS-protected: session.info["tenant_id"]=1 or queries error. Only tenant 1.
- Offload bulk/non-security work to the FREE fleet per Jon's standing directive: `llm -m
  smart-router "..."` / `llm -m gpt-oss-120b "..."` (sync, local) and `mcp__hermes__submit_task`
  (async, free Cloudflare tier). Security-critical code + review stay on Claude. This session
  used both (demo-doc draft on Cloudflare, gutter-math cross-check on smart-router).

VALIDATION STATUS AT HANDOFF: wave code deep-audited (backend + frontend) for gaps/slop/
unwired/stubs — findings + fixes recorded in CONTINUATION-2026-07-17-eve.md. Full backend
suite green. Prod e2e green (branches, gutters exact, daily OH exact, all package tiers match
Greener PDF, config-save unblocked). Verify in prod (proxy + real facts), don't assert.

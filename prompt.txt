Resume perkins-roofing/video-archival after a /clear.

READ FIRST: CONTINUATION-2026-07-23-pm.md (full state). HEAD == ad502e5 (clean, pushed).

⛔ GATE BEFORE ANY PAID 3K RUN: every article must pass ALL compliance checks (the full
Wendy checklist in core/article_criteria.py). The generative loop now enforces this
(jobs.article_job._compliance_gate = a real loop that re-ensures/repairs/re-refines until
green, else BLOCKS). It is NOT yet proven at 100% end-to-end. DO NOT run the 3k
generation until a validation batch shows compliance_rate == 1.0, criteria_failures == {}.

CAN CLEAR NOW: YES. The val4 validation batch is a detached nohup process (measure-mode,
ephemeral — nothing persisted, DB still 66) writing to a file that survives /clear.
- SCRATCH=/tmp/claude-1000/-home-jon-projects-perkins-roofing-video-archival/a9f99bd5-709e-494b-a155-babbcc42071b/scratchpad
- On resume: read $SCRATCH/val4_report.json (compliance verdict). If missing:
  grep -c compliant= $SCRATCH/val4_run.log  (x/12). No keeper work at risk.

FIRST STEPS:
- Cloud SQL proxy (likely still up): GOOGLE_APPLICATION_CREDENTIALS=~/.config/gcloud/perkins-deploy-sa.json
  /tmp/cloud-sql-proxy video-archival-and-content-gen:us-central1:video-archival-and-content-gen-pg --port 5432 &
  PW=$(gcloud secrets versions access latest --secret=db-password);
  DB_URL="postgresql+psycopg://app:${PW}@127.0.0.1:5432/perkins"
- Read val4_report.json. compliance_rate==1.0 → small publish-mode smoke then real run.
  Else fix the named ensure in the loop and re-validate (recipe in continuation).

THE PIPELINE (built today, all pushed):
- core/article_criteria.py = ONE Wendy checklist (16 criteria). Loop gate + batch + tests share it.
- _compliance_gate loops (4 iters: ensures + _apply_repair + LLM re-refine) until 100% or blocks.
  Wired into generate_scored_article AND generate_article (publish path blocks non-compliant live).
- Publish also sets WP category (core/wp_category) + featured image (curated frame) — Wendy criteria.
- jobs/batch_article_job.py = the 3k vehicle. --mode publish [--status draft] [--per-day 10]:
  persist Article row (durable) + WP draft + ScheduledContent paced go-live (promote_job flips live).
  --workers 1 ONLY (8-worker wedges on Vertex). measure mode = no side effects.
- Topic plans: scripts/plan_placement.py (+core/topic_placement.py) = content-hub aware
  (join existing pillar vs new pillar). Plans in $SCRATCH/topic_plan_300.json, placement_plan.json.

COST (measured, Gemini 2.5 Flash): 6 calls, 43k in/24k out per article → 3k ≈ $218 standard /
$109 BATCH. Use batch/Flex for the real run. CF free tier dead for volume.

VALIDATE recipe:
  set -a; source .env; set +a; export GOOGLE_APPLICATION_CREDENTIALS=~/.config/gcloud/perkins-deploy-sa.json
  PW=$(gcloud secrets versions access latest --secret=db-password); export DB_URL="postgresql+psycopg://app:${PW}@127.0.0.1:5432/perkins"
  export PYTHONPATH=$PWD EMBED_BACKEND=vertex GOOGLE_CLOUD_PROJECT=video-archival-and-content-gen GCP_REGION=us-central1 LLM_BACKEND=vertex LLM_MODEL=gemini-2.5-flash
  python -m jobs.batch_article_job $SCRATCH/plan_val.json --workers 1 --no-critique --out $SCRATCH/valN_report.json

ALSO DONE: CI green (fixed 7 tests, trashed 81 CI-fail emails); Metal Warranty plugin+page on
staging (#382; needs Google Maps key referrer allowlist for geocoding); email flood fixed;
Knowify MCP-synced; CF browser_check OFF (#403). Wendy draft w/ WAF note in Outlook — JON SENDS.

GOTCHAS: EMBED_BACKEND=vertex required for grounding; batch --workers 1 only; repair whitelists
SERVICE_SLUGS + the pillar slug (don't regress); GoDaddy WAF blocks Cloud-Run→WP (morpheus OK).
R6: every commit updates docs + Jarvis + memory.

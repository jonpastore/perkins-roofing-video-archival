Resume perkins-roofing/video-archival.

READ FIRST: CONTINUATION-2026-07-27.md.

HEAD 542434f — pushed AND deployed. Prod configs: jupiter v19 / miami v20 / naples v19.
drift_check clean. The Tim email WENT OUT at 16:08 on 2026-07-27 with two attachments.

Confirm:
  git log --oneline origin/main..HEAD          # expect empty
  gcloud run services describe api --region us-central1 \
    --format='value(spec.template.spec.containers[0].image)'   # expect platform:542434f

WHAT IS NOW TRUE (do not re-derive)
  - Profit floor is PER ON-SITE WEEK: $2,500 x ceil(crew_days / 5). Tim's 2026-07-10 email
    works the example himself ("7 days of work ... closer to $5,000 ... 2 weeks"). NOT per job.
  - Accuracy against his day figures is 83% within a day / 0.67 d, measured OUT of sample with
    rule selection nested in the CV loop. 93% was in-sample. Quote 83%.
  - PM incentive: FBC is size-only, HVHZ is project-kind-only. A 35-sq Palm Beach residential
    job takes $100.
  - Shingle overhead $700/day is TIM'S number (his 7/10 and 7/24 emails), not ours.
  - The "how this price was built" section has a checkbox and two audiences; customer mode
    folds base+overhead+profit into one $/sq line so profit cannot be differenced out.

THE WORK, ranked

1. R2 ON THE LAST TWO WAVES — architect AND critic, binding per docs/ENGINEERING_RULES.md,
   still UNRUN. Blocked on a conflict I surfaced rather than resolved: the session directive
   says no subagents, CLAUDE.md mandates them. R2 is what caught the invented
   "Miami 15% / Jupiter 10%" claim. Ask Jon, do not silently skip it again.

2. TIM'S ANSWERS land as config changes. 13 questions went out. The one that can move money
   again: WHAT COUNTS AS A WEEK — we count crew-days / 5; his wording ("in window after
   inspections") suggests calendar occupancy, which is a bigger number on any job with an
   inspection gap.

3. COPY-CONFIG, step 1 — docs/specs/2026-07-27-copy-config-between-branches.md.
   Classify-only, no numbers move, assert none did. Jon's rule: material = shared,
   labour/overhead = branch. Then the badges, which make visible that all 50 keys are
   currently identical across branches and Miami+Naples quote Jupiter's crew rates.

4. SPLIT L FROM M. sloped_base_cost_lm fuses labour and material in one number, so Jon's
   scope rule only reaches about half the money. 29 of Tim's 273 cell comments carry
   L / M / OH / P breakdowns.

5. Jarvis #430 project container, #427 commercial scope model, #418/419/424/428/429/431.

GOTCHAS
- Cloud SQL proxy 127.0.0.1:5432, db `perkins`, user `app` (NOT postgres).
  PW=$(gcloud secrets versions access latest --secret=db-password)
  Every query needs: set app.tenant_id='1'
- CI gates on `ruff check core adapters api jobs` FIRST; tests/ and scripts/ are NOT in the gate.
  Full suite ~20 min; core/ coverage ~97.7% (R1 >= 97%). .coveragerc fail_under=100 is
  pre-existing and fails on a clean tree — do not treat it as your regression.
- `pgrep -f "pytest tests/"` MATCHES YOUR OWN WAITER SHELL. An until-loop on it waits forever.
  Wait on the PID.
- A config change is FOUR places: fixture, prod, tests, and any seeder that could replay the
  old value. e20aa18 did one of four and shipped two failing tests.
- A test docstring is not a source. "6-day week" was justified in one and treated as fact.
- Render a proposal and LOOK at it:
  google-chrome --headless --print-to-pdf=out.pdf file://.../proposal.html && pdftoppm -png
- Never take an all-time median of a price series — slice by period.
- Sweep ALL THREE corpora before asking Tim anything: Zoom transcripts, the 273 cell comments,
  and his 89 emails (Graph $search="from:tim@perkinsroofing.net"; $filter+$orderby together
  400s). The mailbox is the one that got missed. Record zero-hit counts as the evidence a
  question is real.
- Graph attachments are ADDITIVE — delete before re-attaching or the draft carries both.
- bash scripts/deploy.sh (not executable); refuses a dirty tree.

name: ci
on:
  push:
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install deps
        run: pip install -r app/requirements.txt pytest pytest-cov ruff bandit pip-audit
      - name: Lint gate
        run: ruff check core adapters api jobs
      - name: SAST gate (bandit — fail on HIGH)
        run: bandit -r core adapters api jobs app -lll -q
      - name: SCA gate (pip-audit — known CVEs)
        run: pip-audit -r app/requirements.txt
      - name: Test + coverage gate (core >= 97%)
        run: pytest tests/ --cov=core --cov-config=.coveragerc --cov-fail-under=97

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
      - name: Build SPA
        run: |
          cd web
          npm ci
          npm run build
      # The build is a COMPILE check, not a behavioural one. Without this the frontend tests were
      # run by nobody but the author — R1 asks for a behavioural validation on new I/O, and an
      # ungated one is a suggestion. (`npx tsc --noEmit` is likewise not the build gate: it has
      # passed on code `tsc -b` rejected.)
      - name: Test SPA
        run: |
          cd web
          npm test
      - name: SCA gate (npm audit — prod deps, high+)
        run: |
          cd web
          npm audit --omit=dev --audit-level=high

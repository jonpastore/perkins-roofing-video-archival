fastapi>=0.139
python-multipart>=0.0.32
pypdf>=4
uvicorn[standard]>=0.50
pydantic[email]>=2.13
SQLAlchemy>=2.0.51
numpy>=2.5
opencv-python-headless>=4.10  # A2 speaker tracking (YuNet FaceDetectorYN); model committed at adapters/models/
firebase-admin>=7.5
requests>=2.34
google-cloud-storage>=3.12
google-genai>=2.10          # Vertex chat + embeddings; replaced vertexai.*, removed 2026-06-24
google-cloud-aiplatform>=1.159
google-cloud-speech>=2.40
pgvector>=0.5
psycopg[binary]>=3.3
cloud-sql-python-connector>=1.20
pg8000>=1.31
imageio-ffmpeg>=0.6
bleach>=6.4
markdown>=3.10
google-cloud-secret-manager>=2.29
google-cloud-logging>=3.16
jcs==0.2.1
jinja2>=3.1
markupsafe>=2.1
google-cloud-bigquery>=3.0
# adapters/yt_dlp.py shells out to `sys.executable -m yt_dlp`, so this must be an installed
# PACKAGE in the image, not a binary on PATH. It was missing here entirely: enumerate_channel
# had only ever been run from a laptop venv, so scheduling it as a Cloud Run Job (2026-07-28)
# would have failed every night with ModuleNotFoundError. Unpinned deliberately — yt-dlp breaks
# whenever YouTube changes its player and only the newest release keeps working.
yt-dlp>=2026.7

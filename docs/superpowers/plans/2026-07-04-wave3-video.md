# Wave 3 — Video Pipeline Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** From the Content Graph, propose admin-approved 4-7 part mini-series clips of the richest videos, then fuse title + clip + closing screens into a 9:16 1080×1920 reel rendered by ffmpeg on a Cloud Run Job and published to a public GCS URL.

**Architecture:** Fully GCP-native — no external creds. yt-dlp pulls source video; a pure planner ranks/segments; ffmpeg (Cloud Run Job) renders; output lands at a public GCS URL. Render targets satisfy **both** downstream social APIs (Wave 4): 9:16, H.264, ≤300s, ≤300MB (IG's cap is the binding constraint — TikTok allows 4GB/300s).

**Tech Stack:** yt-dlp, ffmpeg (Cloud Run Job), GCS public bucket, Vertex (clip in/out proposal), existing Content Graph.

## Global Constraints
- Inherits Wave 0 constraints.
- **Render spec (binding, to satisfy IG + TikTok):** MP4, H.264 high profile, `+faststart` (moov atom front), 9:16, 1080×1920, 23-60 fps, AAC 128kbps 48kHz, EBU R128 loudness (-14 LUFS), **≤300s duration, ≤300MB size**.
- Clips are **admin-approved before render** — no auto-publish of unreviewed cuts.
- Idempotent render keyed by `series_id + part` — no duplicate renders on retry.
- Music-clean: title/closing screens use license-clean audio (licensed music in source Shorts gets flagged on IG/TT).

---

### Task 1: Mini-series planner (pure, Content-Graph ranked)
**Files:** Create `core/miniseries.py`, migration for `mini_series`; Test `tests/core/test_miniseries.py`
**Interfaces:**
- `mini_series(id, video_id, title, parts_json, approved)`.
- `core/miniseries.py: def rank_candidates(videos_with_graph) -> list[Candidate]` (density = graph-node count / duration, prefer longest/most-robust). `def propose_parts(candidate, graph_nodes, *, min_parts=4, max_parts=7) -> list[Part]` where `Part = {title, start, end}` derived from topic boundaries in the Content Graph.
- [ ] Test ranking prefers high-density long videos; `propose_parts` yields 4-7 parts with non-overlapping in/out inside duration. Implement. Commit.

---

### Task 2: Admin approval API + SPA review screen
**Files:** Create `api/routes/video.py`, `web/src/pages/VideoApprove.tsx`; Test `tests/api/test_video_routes.py`
**Interfaces:** `GET /video/proposals` (pending series), `POST /video/{id}/approve` (admin only, edits parts' in/out then sets `approved=True`). SPA shows proposed parts with editable timecodes + a preview player.
- [ ] Test approve requires `admin` role + persists edited parts. Implement route + screen. Commit.

---

### Task 3: yt-dlp source pull + clip extraction adapter
**Files:** Create `adapters/ffmpeg.py` (clip), extend `adapters/yt_dlp.py`; Test `tests/adapters/test_ffmpeg_clip.py` (5s fixture)
**Interfaces:** `adapters/yt_dlp.py: def pull_video(video_id) -> path`. `adapters/ffmpeg.py: def clip(src, start, end) -> path` (stream-copy where possible).
- [ ] Test clip of a 5s fixture yields the expected duration (±0.1s). Implement. Commit.

---

### Task 4: ffmpeg fuse (title + clip + closing) → 9:16 render
**Files:** Extend `adapters/ffmpeg.py`, Create `core/render_spec.py`, `jobs/render_job.py`; Test `tests/core/test_render_spec.py`, `tests/adapters/test_ffmpeg_fuse.py`
**Interfaces:**
- `core/render_spec.py: def build_filtergraph(parts, title_img, closing_img) -> str` (pure — concat title→clip→closing, scale/pad to 1080×1920, loudnorm). Emits the exact ffmpeg filter string.
- `adapters/ffmpeg.py: def fuse(clip, title_img, closing_img, out) -> path` runs the filtergraph, `-c:v libx264 -profile:v high -movflags +faststart -c:a aac -b:a 128k`.
- `jobs/render_job.py`: idempotent (skip if `social_posts`/GCS object for `series_id+part` exists) → upload to public GCS bucket → return public URL.
- [ ] Test `build_filtergraph` produces a valid 9:16 scale/pad/concat/loudnorm string (assert substrings). Fuse a 5s fixture → assert output is 1080×1920, ≤300s, ≤300MB (ffprobe). Implement. Commit.

---

### Task 5: Publish rendered URL into `scheduled_content` as `kind=reel`
**Files:** Modify `jobs/render_job.py`, reuse Wave-2 `core/scheduler.py`
**Interfaces:** On render success, insert a `scheduled_content(kind="reel", ref_id=<social_posts row>, publish_at, status="scheduled", target)` row so the Wave-2 promoter + Wave-4 social publisher pick it up.
- [ ] Verify a rendered reel appears as a due row for the promoter. Commit.

---

## Self-Review
- Spec coverage: mini-series 4-7 parts ranked by graph density (T1) ✓ · admin approval (T2) ✓ · extract (T3) ✓ · ffmpeg fuse title+clip+closing → 9:16 public GCS URL (T4) ✓ · idempotent (T4) ✓ · feeds scheduler for Wave 4 (T5) ✓.
- Render spec deliberately meets the **binding IG constraint** (9:16, ≤300s, ≤300MB) so Wave-4 publish can't reject the artifact.
- No external creds — buildable + testable in full now (needs GCP for the live Cloud Run Job + public bucket).

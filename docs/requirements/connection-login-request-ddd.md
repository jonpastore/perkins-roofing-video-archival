# DDD: Connection login-request

Status: **implemented-local**

## Domain
A **login request** is an operator-to-operator email, not a credential. It does not mint
tokens. CompanyCam is a key, not a login, so it is outside this context.

# R2 Deep-Review Record - 2026-07-27 (Jarvis #440)

**Summary**
The R2 deep-review for the 2026-07-27 wave never executed; both sub-agents remained idle. Two outstanding items - the test-sabotage audit and the verification of `docs/PRICING_RULES.md` against the shipped configuration - are addressed here. Findings 3.1, 3.3 and 3.4 have been corrected in `docs/PRICING_RULES.md` itself and §11 carries a new warning; 3.2 and 3.5 need engine changes, not doc edits.

Separately, the #433 customer-view leak is fixed and verified on a **rendered** proposal, not just the template: a 35-square Miami tile job renders six customer rows with no PM Incentive, folded into one all-in $40,400 line, summing to the same $45,200 the internal view shows.

## 1. What was verified by hand
- profit floor boundaries: 5.0 crew days -> 1 week, 5.5 -> 2 weeks, no zero floor. **Clean**.
- customer-mode margin leak across four roof types including mixed. **Clean**.
- cross-validation leakage: 0 of 29 folds saw their own target, so the 83 % out-of-sample figure is honest. **Clean**.

## 2. Test-sabotage audit
**Scope** - all `@skip` / `@xfail` markers in `tests/` plus the git diff of `tests/` over the last 20 commits.

### 2.1 (HIGH) `tests/test_f2_engine.py` lines 601-622
Four low-slope tests were marked `@pytest.mark.skip` with reasons that are **false**. The fixture `infra/fixtures/pricing_config_exhibit_b.json` and the three production configs (jupiter v20, miami v21, naples v20) contain populated `low_slope.base_cost_lm` and `tapered_cost_per_sq = 400`. Three of the four tests were empty stubs (`pass`). Result: low-slope pricing shipped with zero engine-level coverage while low-slope quotes were being sent.
**Fix** - the three stubs are now real behavioural tests (insulation adds cost but no profit; tapered adds neither overhead nor profit; low-slope commission is 15 % of profit). The fourth test now uses `skipif` on a missing golden file rather than a false config claim; it also corrects the invalid roof_type ("tpo" -> valid keys are `tpo_adhered` and `tpo_mech_attached`).

### 2.2 (LOW) `tests/api/test_suggestions.py` lines 397, 410
Skipped on "no faqs to paginate" / "no unused videos to paginate". These data-dependent skips can cause pagination assertions to never run. Not sabotage, but the test can report green without exercising the code.

### 2.3 (CLEARED) `tests/test_f2_engine.py` commit e20aa18 - pm_incentive expectation changes
The changes reflect a genuine config update to match Tim's live sheet (FBC keys on size only, HVHZ on project kind only). Each test cites the sheet cell. `test_pm_null_cell_raises` was renamed to `test_pm_null_band_raises` and still asserts the expected raise.

## 3. PRICING_RULES.md vs the shipped config
Method: pulled the three active configs from prod (jupiter v20, miami v21, naples v20), extracted every number in the doc and checked each against all three, then a targeted second pass on what did not match. This doc is client-facing and Tim already has the PDF, so a mismatch is a client-facing error.

### 3.1 (HIGH) `docs/PRICING_RULES.md` line 236 - "Adders" table
"HVHZ upgrade | +$100/sq | tile calculator" - no such adder exists in any shipped config. There is no `HVHZ upgrade` key; actual FBC-to-HVHZ base-cost deltas are +$10/sq (13" tile), +$20/sq (barrel tile), +$270/sq (standing seam metal). `cuts_calc.fixed_per_sq.HVHZ` is null, so the tile calculator is not calibrated for HVHZ. The document promises Tim an adder the engine never applies.

### 3.2 (MED) `docs/PRICING_RULES.md` section 11, lines 295-299 - Commission rules
Document states commission is set per salesperson (15 % for Marco, 7.5 % for Josh). Shipped config stores commission by slope type and zone: `low_slope 0.15`, `sloped 0.10`, `sloped_hvhz null`. The rule described in the doc is not implemented by the engine.

### 3.3 (MED) `docs/PRICING_RULES.md` line 88 - Miami daily rate
Doc lists "about $4,140/day for Miami". Tim's own calculation on 2026-07-27: $85,000/month / 20 work days = $4,250/day. The figure in the doc is stale; the shipped config also lacks $4,250 (see Jarvis #435).

### 3.4 (MED) `docs/PRICING_RULES.md` lines 179, 329 - Small-job profit floor
Both lines still show the $4,000 profit floor as an open question. Tim closed this on 2026-07-27: $2,500 at any size; $4,000 is dead.

### 3.5 (LOW) `docs/PRICING_RULES.md` profit-scale table
The per-square profit band table matches the shipped config. Tim requested on 2026-07-27 to eliminate this mechanism in favor of an operator percentage plus the $2,500 floor (Jarvis #432). Doc and config agree with each other but now conflict with Tim's latest instruction.

**Confirmed matching (single line)** - roof height 3-5 storeys +$1,200 flat, stucco metal $9/LF, WinterGuard $135/sq, 7/12+ tile $305/sq, roof cuts $0/$25/$50, ridge vents $9.79/LF, penetrations $75 each, specialty tile upgrades, PM incentive bands both zones, tile dumpster thresholds FBC 30 / HVHZ 15.

## 4. Still open
- **new_bonus_values ($1,350 fixed)** - whether a customer should see this bonus. Tim's sheet lists bonus values beside permit and delivery; his proposals include a customer-facing "PERKINS BONUS VALUES" stack. Needs Tim's clarification.
- **api/routes/proposals.py:1030** - defaults `calc_audience` to `"internal"` when the field is absent. No live leak today (public accept page strips `calc_lines`; PDF route is staff-gated), but future callers could silently reopen the bug. No test covers this path.
- **498-square commercial TPO golden fixture** - requires an actual quoted total from Tim.

---

## 5. Open items, finished 2026-07-28

Nine items were on the list. Six were stale (§2, §3). Of the rest, three were answered from Tim's
own documents without asking him — the corpus sweep, again, was cheaper than a question.

| item | verdict | where the answer was |
|---|---|---|
| **OI-3** insulation breakpoints | **CLOSED — already implemented** | His sheet rows: `1" Insulation Board ($50 L, $155 M, $50 OH)` = $255, `1 1/2" ($50 L, $175 M, $50 OH)` = $275, `2" ($50 L, $210 M, $50 OH)` = $310, all `-no P`. `insulation_by_thickness` already held exactly those. The open item was generated by reading `insulation_tiers`, the vestigial `[max_sq, price]` key. His comments reconcile the material to the dollar: $85+$70 Olybond = $155, $105+$70 = $175, $140+$70 = $210. |
| **OI-5** plywood — *the one that blocked a quote* | **CLOSED — and the config had the wrong UNIT** | Lumber Schedule, attached to his proposals as a contract exhibit: ⅝" **$120/sheet**, ½" $110, ¾" $145, first 2 sheets free. `deck_types` charges per SQUARE, so filling that key with 120 would have billed $120/sq. Now a top-level per-sheet adder on any roof type. |
| **OI-11** low-slope zones | **NARROWED** | Same prices both zones; the split on his sheet is AVAILABILITY — `BUR Wood (WB-3000 Primer) - not HVHZ (1 story only)` and `BUR Wood (SA V Flashing Strips) - not HVHZ (plywood only)`. Those were `_note_` strings, so an HVHZ job could be quoted on a system his sheet forbids. Now data, and the engine warns. Only the price check against his live calculator remains. |
| **OI-7** commission | open — **not a value question** | Filling the cell answers nothing: the key is per-zone, commission is per-salesperson. Needs a re-key (§3.2). |
| **OI-9** boundary rule | open — **nearly moot** | Consumed only by `profit_per_sq`, which #432 made the legacy path. Rule settled; only the 20-square double-band remained and Tim killed that table. |
| **OI-10** dumpster boundary | **genuinely open** | Zero hits across 273 cell comments, both transcripts and the proposals. A real question for Tim. |
| **OI-12** office burn | **Jon's call** | $4,250 confirmed; `office_men` is a guess worth 13% ($41,318 at 15 men vs $46,964 at 9) against 0.6% for the burn figure itself. |

Also fixed here: the **Lumber Schedule exhibit in every proposal carried no prices** — "charged per
published Perkins schedule", "billed per linear foot by actual size used". It is an exhibit to the
contract, so a customer signing it agreed to rates they could not read. It now states his actual
schedule, and a test asserts the rates are present rather than just the heading.

Config work applied across jupiter/miami/naples and verified at **$0.00 price delta** on a 30-square
job in every branch. `scripts/prune_stale_open_items.py` regenerates the list from the live config
on every run, so an entry cannot outlive its blocker; `scripts/seed_resolved_open_items.py` carries
OI-5 and OI-11 and is inert at default inputs.

**A note on method.** The local-model sweep of all 273 cell comments returned NOT FOUND on all five
questions. It was wrong on three of them: the answers sat in the row CONTEXT rather than the comment
text, and OI-5's answer was in a PDF the sweep never saw. Deterministic greps caught what it missed.
Cheap bulk reading is worth running — it is not worth trusting on a negative.

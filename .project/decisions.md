# Consequential decisions

| ID | Status | Decision / alternatives | Reason / reversal condition | Affected artifacts |
| --- | --- | --- | --- | --- |
| DEC-GL-01 | APPROVED — product owner | Automated sender: `Perkins Roofing — <Branch> Team`; fallback `Perkins Roofing Team`; no individual sender unless an existing approved workflow explicitly requires it. | Team identity avoids unapproved impersonation and scales. Reverse only with product-owner-approved policy/template. | GHL spec, PRD, UIUX, TST |
| DEC-GL-02 | USER DECISION PRESERVED | One corporate GHL location plus one per branch; CallRail out of scope. | Explicit owner direction; revise only on owner change. | UX parity, DDD, TRD |
| DEC-GL-03 | DRAFT TECHNICAL DECISION | App-owned event ledger/outbox and server conditional transitions; initial GHL rollout is read-only mirroring. | Protects payment/lifecycle integrity and provides rollback. Requires consensus and external contract verification before approval. | DDD, TRD, SEC, TST |
| DEC-GL-04 | RESOLVED — product-owner policy / external contract pending | Payment hierarchy: app-native billing events are local canonical ledger; Knowify is the current imported finance/receivables source; GHL is a projection. No source may transition Won/handoff without an authenticated provider contract, immutable transaction/payment ID, and idempotent reconciliation. | Repository evidence shows no processor adapter/settlement contract. Reverse only on verified provider/account evidence and consequential review. | PRD, DOM, TRD, SEC, TST |
| DEC-GL-05 | APPROVED — engineering/product baseline | Raw webhooks: 30 days; normalized integration/audit metadata: one year; required financial/payment records: seven years; active legal/security hold overrides deletion. Minimize PI and never log secrets, authorization headers, or payment credentials. | Baseline unblocks design but is not a legal-compliance claim; legal/privacy validation is pre-production. | TRD, SEC, TST |
| DEC-GL-06 | APPROVED — product owner | Provider-specific terminal-success maps to `DEPOSIT_VERIFIED`; authorization does not. Independently verified payments may accumulate. Before handoff negative events return to payment-pending/review; after irreversible handoff preserve truth and enter an alerted, remediated `PAYMENT_EXCEPTION`/`PAYMENT_AT_RISK`. | Exact provider-state mapping awaits authenticated provider contract. | PRD, DOM, TRD, SEC, TST |
| DEC-GL-07 | APPROVED — product owner | App identity is canonical; GHL is an operational projection. Never destructively apply GHL merge/delete; preserve aliases/IDs/correlation; deterministically reconcile conflict. GHL opt-out governs GHL messaging and the most restrictive consent wins while reconciled. | Reverse only with explicit product-owner policy. | PRD, DOM, UX, SEC, TST |
| DEC-GL-10 | EXTERNAL_POLICY_CONFIG — pre-production configuration | Contractual/legal/business policy is external authority. Software consumes only approved effective policy versions and fails closed to `POLICY_REVIEW_REQUIRED`; it does not interpret contracts or statutes. | Approved contractual input is required before production configuration, but is non-blocking for system design. | PRD, DOM, TRD, SEC, TST |
| DEC-GL-09 | APPROVED — product owner | Tenant-global persons with separately scoped branch operational relationships. Cross-branch references remain least-privilege scoped and audited; GHL merge/delete never bypasses reconciliation. | Prevents duplicate canonical identity while preserving branch isolation. | PRD, DOM, UX, SEC, TST |
| DEC-UX-01 | APPROVED — product owner | Direction A v5 is the final UX design baseline at 97/100. Preserve its hierarchy, parity matrix, role/data scope, responsive behavior, accessibility and interaction contract. | Any material implementation deviation is a UX change proposal; approval does not authorize implementation or release. | UX-DIRECTION-A-V5, TST-UX-DA, PLAN-UX-DA |
